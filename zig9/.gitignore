# Vendored Zig (source + host toolchain) — regenerable via fetch.sh, large.
vendor/
test/_out/

# Python harness caches
__pycache__/
*.pyc

# Native-compiler build artifacts (zig2.c ~213MB, a.outs, logs)
port/plan9/native/_out/
