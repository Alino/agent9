--- a/lib/std/posix.zig
+++ b/lib/std/posix.zig
@@ -5624,6 +5624,9 @@
 
     switch (builtin.object_format) {
         .elf, .c => {},
+        // zig9: Plan 9 has no ELF program headers / dynamic loader — iterate over
+        // nothing. (std.debug.SelfInfo pulls this in; backtraces are just empty.)
+        .plan9 => return,
         else => @compileError("dl_iterate_phdr is not available for this target"),
     }
 
--- a/lib/std/fs.zig
+++ b/lib/std/fs.zig
@@ -656,6 +656,13 @@
                 else => |e| return e,
             };
         },
+        // zig9: Plan 9 has no canonical self-exe path API; best-effort via argv[0].
+        .plan9 => {
+            const arg0 = std.mem.sliceTo(std.os.argv[0], 0);
+            if (arg0.len > out_buffer.len) return error.NameTooLong;
+            @memcpy(out_buffer[0..arg0.len], arg0);
+            return out_buffer[0..arg0.len];
+        },
         else => @compileError("std.fs.selfExePath not supported for this target"),
     }
 }
--- a/lib/std/fs/Dir.zig
+++ b/lib/std/fs/Dir.zig
@@ -852,6 +852,14 @@
         .wasi => .{
             .read = flags.mode != .write_only,
             .write = flags.mode != .read_only,
+        },
+        // zig9: plan9's O struct names its access-mode field `access`, not `ACCMODE`.
+        .plan9 => .{
+            .access = switch (flags.mode) {
+                .read_only => .RDONLY,
+                .write_only => .WRONLY,
+                .read_write => .RDWR,
+            },
         },
         else => .{
             .ACCMODE = switch (flags.mode) {
