#!/bin/bash
# p9 — send a command to the 9front VM via aux/listen1 shell relay
set -euo pipefail
if [ $# -gt 0 ]; then
    {
        printf '%s\n' "$*"
        printf 'exit\n'
    } | nc -w 60 127.0.0.1 1717
else
    {
        cat
        printf '\nexit\n'
    } | nc -w 60 127.0.0.1 1717
fi
