diff --git a/Libraries/LibCore/CMakeLists.txt b/Libraries/LibCore/CMakeLists.txt
index 938d1bc33c..72e58a6fb0 100644
--- a/Libraries/LibCore/CMakeLists.txt
+++ b/Libraries/LibCore/CMakeLists.txt
@@ -68,7 +68,7 @@ else()
 endif()
 
 # FIXME: Implement ProcessStatistics for Windows and *BSD
-if (LINUX)
+if (LINUX AND NOT PLAN9)
     list(APPEND SOURCES Platform/ProcessStatisticsLinux.cpp)
 elseif (APPLE AND NOT IOS)
     list(APPEND SOURCES Platform/ProcessStatisticsMach.cpp)
diff --git a/Libraries/LibCore/Resource.h b/Libraries/LibCore/Resource.h
index 7ab853c488..404657e7c2 100644
--- a/Libraries/LibCore/Resource.h
+++ b/Libraries/LibCore/Resource.h
@@ -12,6 +12,7 @@
 #include <AK/NonnullRefPtr.h>
 #include <AK/Span.h>
 #include <AK/String.h>
+#include <time.h>
 #include <AK/StringView.h>
 #include <AK/Variant.h>
 #include <LibCore/Export.h>
diff --git a/Libraries/LibCore/Socket.cpp b/Libraries/LibCore/Socket.cpp
index 7aecfb1597..0a777bbed8 100644
--- a/Libraries/LibCore/Socket.cpp
+++ b/Libraries/LibCore/Socket.cpp
@@ -508,7 +508,7 @@ ErrorOr<pid_t> LocalSocket::peer_pid() const
 #elif defined(AK_OS_SOLARIS)
     ucred_t* creds = NULL;
     socklen_t creds_size = sizeof(creds);
-#elif defined(AK_OS_GNU_HURD)
+#elif defined(AK_OS_GNU_HURD) || defined(AK_OS_PLAN9)
     return Error::from_errno(ENOTSUP);
 #else
     struct ucred creds = {};
@@ -527,7 +527,7 @@ ErrorOr<pid_t> LocalSocket::peer_pid() const
 #elif defined(AK_OS_SOLARIS)
     TRY(System::getsockopt(m_helper.fd(), SOL_SOCKET, SO_RECVUCRED, &creds, &creds_size));
     return ucred_getpid(creds);
-#elif !defined(AK_OS_GNU_HURD)
+#elif !defined(AK_OS_GNU_HURD) && !defined(AK_OS_PLAN9)
     TRY(System::getsockopt(m_helper.fd(), SOL_SOCKET, SO_PEERCRED, &creds, &creds_size));
     return creds.pid;
 #endif
diff --git a/Libraries/LibCore/StandardPaths.cpp b/Libraries/LibCore/StandardPaths.cpp
index 9b6c68c5f1..f7a71991a7 100644
--- a/Libraries/LibCore/StandardPaths.cpp
+++ b/Libraries/LibCore/StandardPaths.cpp
@@ -38,10 +38,17 @@ ByteString StandardPaths::home_directory()
 #else
     if (auto* home_env = getenv("HOME"))
         return LexicalPath::canonicalized_path(home_env);
-
+#    if defined(AK_OS_PLAN9)
+    // Plan 9 names the home env var "home" (lowercase), not "HOME", and has
+    // no passwd db (getpwuid is a stub).
+    if (auto* plan9_home = getenv("home"))
+        return LexicalPath::canonicalized_path(plan9_home);
+    ByteString path = "/usr/glenda";
+#    else
     auto* pwd = getpwuid(getuid());
     ByteString path = pwd ? pwd->pw_dir : "/";
     endpwent();
+#    endif
 #endif
     return LexicalPath::canonicalized_path(path);
 }
diff --git a/Libraries/LibCore/System.cpp b/Libraries/LibCore/System.cpp
index fadcfc933d..fc0f3d3563 100644
--- a/Libraries/LibCore/System.cpp
+++ b/Libraries/LibCore/System.cpp
@@ -23,6 +23,17 @@
 #include <termios.h>
 #include <unistd.h>
 
+#if defined(AK_OS_PLAN9)
+// cc9 runtime: kernel path of an open fd (fd2path(2)) + demand-paged
+// VA-reservation segments (segattach/segfree/segdetach; see cc9 shm9.c
+// siblings). These realize reserve/commit/decommit/release below.
+extern "C" long cc9_fd2path_for_exe(int, char*, unsigned long);
+extern "C" void* cc9_reserve_va_segment(unsigned long);
+extern "C" long cc9_segfree_pages(void*, unsigned long);
+extern "C" long cc9_segdetach_va(void*);
+extern "C" int cc9_shm_create(unsigned long);
+#endif
+
 #if defined(AK_OS_LINUX) && !defined(MFD_CLOEXEC)
 #    include <linux/memfd.h>
 #    include <sys/syscall.h>
@@ -144,22 +155,42 @@ ErrorOr<void> munmap(void* address, size_t size)
 
 ErrorOr<void*> reserve_address_space(size_t size)
 {
+#if defined(AK_OS_PLAN9)
+    // A demand-paged anonymous segment: costs address space until touched,
+    // then pages commit lazily — the closest native to PROT_NONE+NORESERVE.
+    // (Pages are RW from the start; the PROT_NONE guard property is not
+    // enforced.) segattach length is a 32-bit ulong: reject what can't fit.
+    if (size >= (4ull * GiB))
+        return Error::from_errno(ENOMEM);
+    void* ptr = cc9_reserve_va_segment(size);
+    if (ptr == nullptr)
+        return Error::from_syscall("segattach"sv, ENOMEM);
+    return ptr;
+#else
     int flags = MAP_PRIVATE | MAP_ANONYMOUS;
-#ifdef MAP_NORESERVE
+#    ifdef MAP_NORESERVE
     flags |= MAP_NORESERVE;
-#endif
+#    endif
     auto* ptr = ::mmap(nullptr, size, PROT_NONE, flags, -1, 0);
     if (ptr == MAP_FAILED)
         return Error::from_syscall("mmap"sv, errno);
     return ptr;
+#endif
 }
 
 ErrorOr<void> commit_memory(void* address, size_t size)
 {
+#if defined(AK_OS_PLAN9)
+    // Reserved segments demand-commit on touch; nothing to do.
+    (void)address;
+    (void)size;
+    return {};
+#else
     auto* ptr = ::mmap(address, size, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS | MAP_FIXED, -1, 0);
     if (ptr == MAP_FAILED)
         return Error::from_syscall("mmap"sv, errno);
     return {};
+#endif
 }
 
 ErrorOr<void> decommit_memory(void* address, size_t size)
@@ -167,6 +198,12 @@ ErrorOr<void> decommit_memory(void* address, size_t size)
     if (size == 0)
         return {};
 
+#if defined(AK_OS_PLAN9)
+    // Return the pages to the kernel; the VA stays attached (segfree(2)).
+    if (cc9_segfree_pages(address, size) < 0)
+        return Error::from_syscall("segfree"sv, EINVAL);
+    return {};
+#else
     int flags = MAP_PRIVATE | MAP_ANONYMOUS | MAP_FIXED;
 #ifdef MAP_NORESERVE
     flags |= MAP_NORESERVE;
@@ -176,19 +213,42 @@ ErrorOr<void> decommit_memory(void* address, size_t size)
         return Error::from_syscall("mmap"sv, errno);
     VERIFY(ptr == address);
     return {};
+#endif
 }
 
 ErrorOr<void> release_address_space(void* address, size_t size)
 {
+#if defined(AK_OS_PLAN9)
+    if (cc9_segdetach_va(address) < 0)
+        return Error::from_syscall("segdetach"sv, EINVAL);
+    (void)size;
+    return {};
+#else
     if (::munmap(address, size) < 0)
         return Error::from_syscall("munmap"sv, errno);
     return {};
+#endif
 }
 
 ErrorOr<int> anon_create([[maybe_unused]] size_t size, [[maybe_unused]] int options)
 {
     int fd = -1;
-#if defined(AK_OS_LINUX) || defined(AK_OS_FREEBSD)
+#if defined(AK_OS_PLAN9)
+    // A named global segment (#g); the fd is its data file, already sized —
+    // no ftruncate (and none is possible), so return directly. mmap
+    // MAP_SHARED of this fd attaches the segment (real cross-process pages).
+    fd = cc9_shm_create(size);
+    if (fd < 0)
+        return Error::from_errno(errno);
+    if ((options & O_CLOEXEC) == 0) {
+        if (::fcntl(fd, F_SETFD, 0) == -1) {
+            auto saved_errno = errno;
+            TRY(close(fd));
+            return Error::from_errno(saved_errno);
+        }
+    }
+    return fd;
+#elif defined(AK_OS_LINUX) || defined(AK_OS_FREEBSD)
     // FIXME: Support more options on Linux.
     auto linux_options = ((options & O_CLOEXEC) > 0) ? MFD_CLOEXEC : 0;
     fd = memfd_create("", linux_options);
@@ -799,6 +859,20 @@ ErrorOr<ByteString> current_executable_path()
     if (sizeof(info.name) > sizeof(path))
         return Error::from_errno(ENAMETOOLONG);
     strlcpy(path, info.name, sizeof(path) - 1);
+#elif defined(AK_OS_PLAN9)
+    // /proc/<pid>/text is an open handle on the running executable; the
+    // kernel knows its path and cc9's fd2path shim surfaces it.
+    {
+        char textpath[64];
+        (void)snprintf(textpath, sizeof(textpath), "/proc/%d/text", ::getpid());
+        int tfd = ::open(textpath, O_RDONLY);
+        if (tfd < 0)
+            return Error::from_errno(errno);
+        long rc = cc9_fd2path_for_exe(tfd, path, sizeof(path) - 1);
+        ::close(tfd);
+        if (rc < 0)
+            return Error::from_string_literal("current_executable_path: fd2path failed");
+    }
 #else
 #    warning "Not sure how to get current_executable_path on this platform!"
     // GetModuleFileName on Windows, unsure about OpenBSD.
diff --git a/Libraries/LibCore/System.h b/Libraries/LibCore/System.h
index 3b2de6a921..e4576c227e 100644
--- a/Libraries/LibCore/System.h
+++ b/Libraries/LibCore/System.h
@@ -46,7 +46,7 @@ struct addrinfo;
 struct sockaddr;
 #endif
 
-#if !defined(AK_OS_BSD_GENERIC) && !defined(AK_OS_ANDROID) && !defined(AK_OS_WINDOWS)
+#if !defined(AK_OS_BSD_GENERIC) && !defined(AK_OS_ANDROID) && !defined(AK_OS_WINDOWS) && !defined(AK_OS_PLAN9)
 #    include <shadow.h>
 #endif
 
diff --git a/Libraries/LibCore/SystemServerTakeover.cpp b/Libraries/LibCore/SystemServerTakeover.cpp
index 09047f66ff..fe41c4292a 100644
--- a/Libraries/LibCore/SystemServerTakeover.cpp
+++ b/Libraries/LibCore/SystemServerTakeover.cpp
@@ -62,8 +62,12 @@ ErrorOr<NonnullOwnPtr<Core::LocalSocket>> take_over_socket_from_system_server(By
     }
 
     // Sanity check: it has to be a socket.
+    // (Plan 9: socketpairs ARE full-duplex pipes; there is no S_IFSOCK to
+    // check for, so accept the fd as-is.)
+#if !defined(AK_OS_PLAN9)
     if (!System::is_socket(fd))
         return Error::from_string_literal("The fd or handle we got from SystemServer is not a socket");
+#endif
 
     auto socket = TRY(Core::LocalSocket::adopt_fd(fd));
     // It had to be !CLOEXEC for obvious reasons, but we
