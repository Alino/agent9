diff --git a/Libraries/LibIPC/CMakeLists.txt b/Libraries/LibIPC/CMakeLists.txt
index 0970926585..e87720cc2b 100644
--- a/Libraries/LibIPC/CMakeLists.txt
+++ b/Libraries/LibIPC/CMakeLists.txt
@@ -15,6 +15,10 @@ if (APPLE AND NOT IOS)
         MachBootstrapListener.cpp
         TransportBootstrapMach.cpp
         TransportMachPort.cpp)
+elseif (PLAN9)
+    list(APPEND SOURCES
+        Attachment.cpp
+        TransportPlan9.cpp)
 elseif (UNIX)
     list(APPEND SOURCES
         Attachment.cpp
diff --git a/Libraries/LibJS/CMakeLists.txt b/Libraries/LibJS/CMakeLists.txt
index 25d61bc1eb..07b80c7f1d 100644
--- a/Libraries/LibJS/CMakeLists.txt
+++ b/Libraries/LibJS/CMakeLists.txt
@@ -340,35 +340,53 @@ if (DEFINED ASMINT_ARCH)
     set(ASMINT_GENERATED_S "${CMAKE_CURRENT_BINARY_DIR}/Bytecode/AsmInterpreter/asmint_${ASMINT_ARCH}.S")
     set(ASM_OFFSETS_FILE "${CMAKE_CURRENT_BINARY_DIR}/Bytecode/AsmInterpreter/asm_offsets.conf")
 
-    # Build-time offset generator: compiles a small C++ program that uses
-    # offsetof() to emit struct field offsets as DSL constants.
-    add_executable(gen_asm_offsets "${CMAKE_CURRENT_SOURCE_DIR}/Bytecode/AsmInterpreter/gen_asm_offsets.cpp")
-    target_link_libraries(gen_asm_offsets PRIVATE AK)
-    if (MSVC)
-        target_compile_definitions(gen_asm_offsets PRIVATE _ALLOW_KEYWORD_MACROS)
-    endif()
-    target_compile_definitions(gen_asm_offsets PRIVATE private=public protected=public)
-    # NB: Strip fuzzer sanitizer flags so gen_asm_offsets can define its own main().
-    if (ENABLE_FUZZERS_LIBFUZZER)
-        target_compile_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
-        target_link_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
-    endif()
-    # NB: gen_asm_offsets no longer depends on Op.h since opcode field
-    # offsets are now computed from Bytecode.def by asmintgen directly.
+    # Cross builds whose target binaries cannot run on the build host may
+    # provide a pre-generated offsets file (LADYBIRD_ASM_OFFSETS_FILE) and a
+    # host-built asmintgen (LADYBIRD_ASMINTGEN). The offsets file must be
+    # ABI-verified against the target toolchain (ladybird9: 123 static_asserts
+    # compiled by the target compiler prove it byte-exact).
+    if (LADYBIRD_ASM_OFFSETS_FILE)
+        add_custom_command(
+            OUTPUT "${ASM_OFFSETS_FILE}"
+            COMMAND "${CMAKE_COMMAND}" -E copy_if_different "${LADYBIRD_ASM_OFFSETS_FILE}" "${ASM_OFFSETS_FILE}"
+            DEPENDS "${LADYBIRD_ASM_OFFSETS_FILE}"
+            COMMENT "Using pre-generated asm struct offsets"
+        )
+    else()
+        # Build-time offset generator: compiles a small C++ program that uses
+        # offsetof() to emit struct field offsets as DSL constants.
+        add_executable(gen_asm_offsets "${CMAKE_CURRENT_SOURCE_DIR}/Bytecode/AsmInterpreter/gen_asm_offsets.cpp")
+        target_link_libraries(gen_asm_offsets PRIVATE AK)
+        if (MSVC)
+            target_compile_definitions(gen_asm_offsets PRIVATE _ALLOW_KEYWORD_MACROS)
+        endif()
+        target_compile_definitions(gen_asm_offsets PRIVATE private=public protected=public)
+        # NB: Strip fuzzer sanitizer flags so gen_asm_offsets can define its own main().
+        if (ENABLE_FUZZERS_LIBFUZZER)
+            target_compile_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
+            target_link_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
+        endif()
+        # NB: gen_asm_offsets no longer depends on Op.h since opcode field
+        # offsets are now computed from Bytecode.def by asmintgen directly.
 
-    add_custom_command(
-        OUTPUT "${ASM_OFFSETS_FILE}"
-        COMMAND "$<TARGET_FILE:gen_asm_offsets>" > "${ASM_OFFSETS_FILE}"
-        DEPENDS gen_asm_offsets
-        COMMENT "Generating asm struct offsets"
-    )
+        add_custom_command(
+            OUTPUT "${ASM_OFFSETS_FILE}"
+            COMMAND "$<TARGET_FILE:gen_asm_offsets>" > "${ASM_OFFSETS_FILE}"
+            DEPENDS gen_asm_offsets
+            COMMENT "Generating asm struct offsets"
+        )
+    endif()
 
-    build_rust_binary(
-        MANIFEST_PATH AsmIntGen/Cargo.toml
-        CRATE_NAME asmintgen
-        BINARY_NAME asmintgen
-        OUTPUT_PATH_VAR ASMINTGEN_BIN
-    )
+    if (LADYBIRD_ASMINTGEN)
+        set(ASMINTGEN_BIN "${LADYBIRD_ASMINTGEN}")
+    else()
+        build_rust_binary(
+            MANIFEST_PATH AsmIntGen/Cargo.toml
+            CRATE_NAME asmintgen
+            BINARY_NAME asmintgen
+            OUTPUT_PATH_VAR ASMINTGEN_BIN
+        )
+    endif()
 
     set(BYTECODE_DEF "${CMAKE_CURRENT_SOURCE_DIR}/Bytecode/Bytecode.def")
 
diff --git a/Meta/CMake/check_for_dependencies.cmake b/Meta/CMake/check_for_dependencies.cmake
index 311dfa0fc0..5b0fa73a58 100644
--- a/Meta/CMake/check_for_dependencies.cmake
+++ b/Meta/CMake/check_for_dependencies.cmake
@@ -2,7 +2,8 @@ include_guard()
 
 find_package(PkgConfig REQUIRED)
 
-if (NOT WIN32 AND NOT ANDROID)
+if (NOT WIN32 AND NOT ANDROID AND NOT PLAN9)
+    # PLAN9: no terminfo; the js REPL's line editing is deferred (raw stdin).
     pkg_check_modules(LIBEDIT REQUIRED IMPORTED_TARGET libedit)
 endif()
 
@@ -26,7 +27,10 @@ find_path(WUFFS_INCLUDE_DIR NAMES wuffs/wuffs-v0.3.c REQUIRED)
 pkg_check_modules(WOFF2 REQUIRED IMPORTED_TARGET libwoff2dec)
 
 # TODO: Figure out if we can do this the same way on all platforms
-if (NOT ANDROID)
+if (PLAN9)
+    # No <video>/<audio> decode yet (LibMedia stub) and no JPEG XL — both
+    # tracked as parity deferrals in ladybird9/parity/.
+elseif (NOT ANDROID)
     pkg_check_modules(Jxl REQUIRED IMPORTED_TARGET libjxl)
 
     pkg_check_modules(AVCODEC REQUIRED IMPORTED_TARGET libavcodec)
@@ -40,7 +44,9 @@ else()
     find_package(FFMPEG REQUIRED)
 endif()
 
-if (NOT APPLE AND NOT ANDROID AND NOT WIN32)
+if (NOT APPLE AND NOT ANDROID AND NOT WIN32 AND NOT PLAN9)
+    # PLAN9: fonts come from a directory-scan provider + Skia's custom-dir
+    # FreeType fontmgr; no fontconfig on 9front.
     find_package(Fontconfig REQUIRED)
     set(HAS_FONTCONFIG ON CACHE BOOL "" FORCE)
     add_cxx_compile_definitions(USE_FONTCONFIG=1)
diff --git a/Tests/LibIPC/CMakeLists.txt b/Tests/LibIPC/CMakeLists.txt
index 5df47617d6..b791d707af 100644
--- a/Tests/LibIPC/CMakeLists.txt
+++ b/Tests/LibIPC/CMakeLists.txt
@@ -1,4 +1,8 @@
 if (UNIX AND NOT APPLE)
-    ladybird_test("TestTransportSocket.cpp" LibIPC LIBS LibIPC LibSync)
+    if (NOT PLAN9)
+        # Exercises TransportSocket internals (SendQueue, drain windows);
+        # plan9 uses TransportPlan9, covered by the portable TestConnection.
+        ladybird_test("TestTransportSocket.cpp" LibIPC LIBS LibIPC LibSync)
+    endif()
     ladybird_test("TestConnection.cpp" LibIPC LIBS LibIPC)
 endif()
diff --git a/Utilities/CMakeLists.txt b/Utilities/CMakeLists.txt
index caa5dadebd..0bc6ca3616 100644
--- a/Utilities/CMakeLists.txt
+++ b/Utilities/CMakeLists.txt
@@ -1,7 +1,7 @@
 ladybird_utility(js SOURCES js.cpp LIBS LibCrypto LibJS LibMain LibTextCodec LibGC)
 ladybird_utility(wasm SOURCES wasm.cpp LIBS LibFileSystem LibWasm LibMain LibJS LibCrypto LibGC)
 
-if (NOT WIN32 AND NOT ANDROID)
+if (NOT WIN32 AND NOT ANDROID AND NOT PLAN9)
     target_link_libraries(js PRIVATE PkgConfig::LIBEDIT)
 endif()
 
