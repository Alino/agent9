diff --git a/Libraries/LibIPC/CMakeLists.txt b/Libraries/LibIPC/CMakeLists.txt
index 0970926585..e87720cc2b 100644
--- a/Libraries/LibIPC/CMakeLists.txt
+++ b/Libraries/LibIPC/CMakeLists.txt
@@ -15,6 +15,10 @@ if (APPLE AND NOT IOS)
         MachBootstrapListener.cpp
         TransportBootstrapMach.cpp
         TransportMachPort.cpp)
+elseif (PLAN9)
+    list(APPEND SOURCES
+        Attachment.cpp
+        TransportPlan9.cpp)
 elseif (UNIX)
     list(APPEND SOURCES
         Attachment.cpp
diff --git a/Libraries/LibImageDecoders/CMakeLists.txt b/Libraries/LibImageDecoders/CMakeLists.txt
index cd06caf427..756dac7674 100644
--- a/Libraries/LibImageDecoders/CMakeLists.txt
+++ b/Libraries/LibImageDecoders/CMakeLists.txt
@@ -31,9 +31,15 @@ target_link_libraries(LibImageDecoders PRIVATE
     WebP::webpdecoder
     WebP::webpdemux
     WebP::libwebpmux
-    avif
 )
 
+if (PLAN9)
+    # AVIF via pkg-config (build-libavif.sh); pulls dav1d transitively.
+    target_link_libraries(LibImageDecoders PRIVATE PkgConfig::AVIF)
+else()
+    target_link_libraries(LibImageDecoders PRIVATE avif)
+endif()
+
 target_include_directories(LibImageDecoders PRIVATE ${WUFFS_INCLUDE_DIR})
 
 set(generated_sources TIFFMetadata.h TIFFTagHandler.cpp)
@@ -68,8 +74,10 @@ if (NOT LIBPNG_HAS_APNG)
     message(FATAL_ERROR "libpng does not support APNG, which is required by LibImageDecoders.")
 endif()
 
-if (NOT ANDROID)
-    target_link_libraries(LibImageDecoders PRIVATE PkgConfig::Jxl)
-else()
+if (ANDROID)
     target_link_libraries(LibImageDecoders PRIVATE libjxl::libjxl hwy::hwy)
+else()
+    # PLAN9 + Linux: libjxl via pkg-config (PLAN9 build-libjxl.sh installs
+    # libjxl.pc with Requires: libhwy libbrotli* libjxl_cms).
+    target_link_libraries(LibImageDecoders PRIVATE PkgConfig::Jxl)
 endif()
diff --git a/Libraries/LibJS/CMakeLists.txt b/Libraries/LibJS/CMakeLists.txt
index 25d61bc1eb..07b80c7f1d 100644
--- a/Libraries/LibJS/CMakeLists.txt
+++ b/Libraries/LibJS/CMakeLists.txt
@@ -340,35 +340,53 @@ if (DEFINED ASMINT_ARCH)
     set(ASMINT_GENERATED_S "${CMAKE_CURRENT_BINARY_DIR}/Bytecode/AsmInterpreter/asmint_${ASMINT_ARCH}.S")
     set(ASM_OFFSETS_FILE "${CMAKE_CURRENT_BINARY_DIR}/Bytecode/AsmInterpreter/asm_offsets.conf")
 
-    # Build-time offset generator: compiles a small C++ program that uses
-    # offsetof() to emit struct field offsets as DSL constants.
-    add_executable(gen_asm_offsets "${CMAKE_CURRENT_SOURCE_DIR}/Bytecode/AsmInterpreter/gen_asm_offsets.cpp")
-    target_link_libraries(gen_asm_offsets PRIVATE AK)
-    if (MSVC)
-        target_compile_definitions(gen_asm_offsets PRIVATE _ALLOW_KEYWORD_MACROS)
-    endif()
-    target_compile_definitions(gen_asm_offsets PRIVATE private=public protected=public)
-    # NB: Strip fuzzer sanitizer flags so gen_asm_offsets can define its own main().
-    if (ENABLE_FUZZERS_LIBFUZZER)
-        target_compile_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
-        target_link_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
-    endif()
-    # NB: gen_asm_offsets no longer depends on Op.h since opcode field
-    # offsets are now computed from Bytecode.def by asmintgen directly.
+    # Cross builds whose target binaries cannot run on the build host may
+    # provide a pre-generated offsets file (LADYBIRD_ASM_OFFSETS_FILE) and a
+    # host-built asmintgen (LADYBIRD_ASMINTGEN). The offsets file must be
+    # ABI-verified against the target toolchain (ladybird9: 123 static_asserts
+    # compiled by the target compiler prove it byte-exact).
+    if (LADYBIRD_ASM_OFFSETS_FILE)
+        add_custom_command(
+            OUTPUT "${ASM_OFFSETS_FILE}"
+            COMMAND "${CMAKE_COMMAND}" -E copy_if_different "${LADYBIRD_ASM_OFFSETS_FILE}" "${ASM_OFFSETS_FILE}"
+            DEPENDS "${LADYBIRD_ASM_OFFSETS_FILE}"
+            COMMENT "Using pre-generated asm struct offsets"
+        )
+    else()
+        # Build-time offset generator: compiles a small C++ program that uses
+        # offsetof() to emit struct field offsets as DSL constants.
+        add_executable(gen_asm_offsets "${CMAKE_CURRENT_SOURCE_DIR}/Bytecode/AsmInterpreter/gen_asm_offsets.cpp")
+        target_link_libraries(gen_asm_offsets PRIVATE AK)
+        if (MSVC)
+            target_compile_definitions(gen_asm_offsets PRIVATE _ALLOW_KEYWORD_MACROS)
+        endif()
+        target_compile_definitions(gen_asm_offsets PRIVATE private=public protected=public)
+        # NB: Strip fuzzer sanitizer flags so gen_asm_offsets can define its own main().
+        if (ENABLE_FUZZERS_LIBFUZZER)
+            target_compile_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
+            target_link_options(gen_asm_offsets PRIVATE -fno-sanitize=fuzzer)
+        endif()
+        # NB: gen_asm_offsets no longer depends on Op.h since opcode field
+        # offsets are now computed from Bytecode.def by asmintgen directly.
 
-    add_custom_command(
-        OUTPUT "${ASM_OFFSETS_FILE}"
-        COMMAND "$<TARGET_FILE:gen_asm_offsets>" > "${ASM_OFFSETS_FILE}"
-        DEPENDS gen_asm_offsets
-        COMMENT "Generating asm struct offsets"
-    )
+        add_custom_command(
+            OUTPUT "${ASM_OFFSETS_FILE}"
+            COMMAND "$<TARGET_FILE:gen_asm_offsets>" > "${ASM_OFFSETS_FILE}"
+            DEPENDS gen_asm_offsets
+            COMMENT "Generating asm struct offsets"
+        )
+    endif()
 
-    build_rust_binary(
-        MANIFEST_PATH AsmIntGen/Cargo.toml
-        CRATE_NAME asmintgen
-        BINARY_NAME asmintgen
-        OUTPUT_PATH_VAR ASMINTGEN_BIN
-    )
+    if (LADYBIRD_ASMINTGEN)
+        set(ASMINTGEN_BIN "${LADYBIRD_ASMINTGEN}")
+    else()
+        build_rust_binary(
+            MANIFEST_PATH AsmIntGen/Cargo.toml
+            CRATE_NAME asmintgen
+            BINARY_NAME asmintgen
+            OUTPUT_PATH_VAR ASMINTGEN_BIN
+        )
+    endif()
 
     set(BYTECODE_DEF "${CMAKE_CURRENT_SOURCE_DIR}/Bytecode/Bytecode.def")
 
diff --git a/Libraries/LibMedia/CMakeLists.txt b/Libraries/LibMedia/CMakeLists.txt
index 9cd10e14d0..ebfd2a3c23 100644
--- a/Libraries/LibMedia/CMakeLists.txt
+++ b/Libraries/LibMedia/CMakeLists.txt
@@ -40,21 +40,27 @@ set(SOURCES
 ladybird_lib(LibMedia media EXPLICIT_SYMBOL_EXPORT)
 target_link_libraries(LibMedia PRIVATE LibCore LibCrypto LibIPC LibGfx LibSync LibThreading LibUnicode)
 
-target_sources(LibMedia PRIVATE
-        FFmpeg/FFmpegAudioConverter.cpp
-        FFmpeg/FFmpegAudioDecoder.cpp
-        FFmpeg/FFmpegDemuxer.cpp
-        FFmpeg/FFmpegHelpers.cpp
-        FFmpeg/FFmpegIOContext.cpp
-        FFmpeg/FFmpegVideoDecoder.cpp
-)
-
-if (NOT ANDROID)
-    target_link_libraries(LibMedia PRIVATE PkgConfig::AVCODEC PkgConfig::AVFORMAT PkgConfig::AVUTIL PkgConfig::LIBSWRESAMPLE)
+if (FALSE)
+    # (was PLAN9 stub) ffmpeg 7.1.1 cross-built C-only into the sysroot
+    # (host/deps/build-ffmpeg.sh); PLAN9 now links the real decoders.
+    target_sources(LibMedia PRIVATE FFmpeg/FFmpegStubPlan9.cpp)
 else()
-    target_include_directories(LibMedia PRIVATE ${FFMPEG_INCLUDE_DIRS})
-    target_link_directories(LibMedia PRIVATE ${FFMPEG_LIBRARY_DIRS})
-    target_link_libraries(LibMedia PRIVATE ${FFMPEG_LIBRARIES})
+    target_sources(LibMedia PRIVATE
+            FFmpeg/FFmpegAudioConverter.cpp
+            FFmpeg/FFmpegAudioDecoder.cpp
+            FFmpeg/FFmpegDemuxer.cpp
+            FFmpeg/FFmpegHelpers.cpp
+            FFmpeg/FFmpegIOContext.cpp
+            FFmpeg/FFmpegVideoDecoder.cpp
+    )
+
+    if (NOT ANDROID)
+        target_link_libraries(LibMedia PRIVATE PkgConfig::AVCODEC PkgConfig::AVFORMAT PkgConfig::AVUTIL PkgConfig::LIBSWRESAMPLE)
+    else()
+        target_include_directories(LibMedia PRIVATE ${FFMPEG_INCLUDE_DIRS})
+        target_link_directories(LibMedia PRIVATE ${FFMPEG_LIBRARY_DIRS})
+        target_link_libraries(LibMedia PRIVATE ${FFMPEG_LIBRARIES})
+    endif()
 endif()
 
 if (LADYBIRD_AUDIO_BACKEND STREQUAL "PULSE")
diff --git a/Libraries/LibSandbox/CMakeLists.txt b/Libraries/LibSandbox/CMakeLists.txt
index c4b12d475b..4f65680388 100644
--- a/Libraries/LibSandbox/CMakeLists.txt
+++ b/Libraries/LibSandbox/CMakeLists.txt
@@ -2,7 +2,8 @@ set(SOURCES
     Sandbox.cpp
 )
 
-if (LINUX)
+# PLAN9: CMAKE_SYSTEM_NAME is Linux (toolchain seam) but there is no seccomp.
+if (LINUX AND NOT PLAN9)
     list(APPEND SOURCES
         Seccomp.cpp
     )
diff --git a/Meta/CMake/check_for_dependencies.cmake b/Meta/CMake/check_for_dependencies.cmake
index 311dfa0fc0..2a8af8f430 100644
--- a/Meta/CMake/check_for_dependencies.cmake
+++ b/Meta/CMake/check_for_dependencies.cmake
@@ -2,7 +2,8 @@ include_guard()
 
 find_package(PkgConfig REQUIRED)
 
-if (NOT WIN32 AND NOT ANDROID)
+if (NOT WIN32 AND NOT ANDROID AND NOT PLAN9)
+    # PLAN9: no terminfo; the js REPL's line editing is deferred (raw stdin).
     pkg_check_modules(LIBEDIT REQUIRED IMPORTED_TARGET libedit)
 endif()
 
@@ -26,7 +27,25 @@ find_path(WUFFS_INCLUDE_DIR NAMES wuffs/wuffs-v0.3.c REQUIRED)
 pkg_check_modules(WOFF2 REQUIRED IMPORTED_TARGET libwoff2dec)
 
 # TODO: Figure out if we can do this the same way on all platforms
-if (NOT ANDROID)
+if (PLAN9)
+    # JPEG XL (libjxl) + AVIF (libavif over dav1d) cross-built into the sysroot
+    # (host/deps/build-{highway,libjxl,dav1d,libavif}.sh); both link via
+    # pkg-config so the static deps' transitive libs reach the link line.
+    # <video>/<audio> (ffmpeg) still deferred (LibMedia stub) — parity ledger.
+    pkg_check_modules(Jxl REQUIRED IMPORTED_TARGET libjxl)
+    pkg_check_modules(AVIF REQUIRED IMPORTED_TARGET libavif)
+    # Fully-static link: libavif.pc lists dav1d only in Requires.private, which
+    # the dynamic imported target drops. Pull it onto the target explicitly
+    # (same fix as CURL's nghttp2/zstd below).
+    find_library(LB9_DAV1D_LIB dav1d REQUIRED)
+    set_property(TARGET PkgConfig::AVIF APPEND PROPERTY INTERFACE_LINK_LIBRARIES "${LB9_DAV1D_LIB}")
+
+    # ffmpeg 7.1.1 (C-only, host/deps/build-ffmpeg.sh) for <video>/<audio> decode.
+    pkg_check_modules(AVCODEC REQUIRED IMPORTED_TARGET libavcodec)
+    pkg_check_modules(AVFORMAT REQUIRED IMPORTED_TARGET libavformat)
+    pkg_check_modules(AVUTIL REQUIRED IMPORTED_TARGET libavutil)
+    pkg_check_modules(LIBSWRESAMPLE REQUIRED IMPORTED_TARGET libswresample)
+elseif (NOT ANDROID)
     pkg_check_modules(Jxl REQUIRED IMPORTED_TARGET libjxl)
 
     pkg_check_modules(AVCODEC REQUIRED IMPORTED_TARGET libavcodec)
@@ -40,7 +59,9 @@ else()
     find_package(FFMPEG REQUIRED)
 endif()
 
-if (NOT APPLE AND NOT ANDROID AND NOT WIN32)
+if (NOT APPLE AND NOT ANDROID AND NOT WIN32 AND NOT PLAN9)
+    # PLAN9: fonts come from a directory-scan provider + Skia's custom-dir
+    # FreeType fontmgr; no fontconfig on 9front.
     find_package(Fontconfig REQUIRED)
     set(HAS_FONTCONFIG ON CACHE BOOL "" FORCE)
     add_cxx_compile_definitions(USE_FONTCONFIG=1)
@@ -72,10 +93,33 @@ else()
 endif()
 
 find_package(CURL REQUIRED)
+if (PLAN9)
+    # PLAN9: fully-static link; the static libcurl.a's private deps (HTTP/2,
+    # zstd) must reach the final link line — the CURL package config from the
+    # static cross-build doesn't carry them.
+    find_library(LB9_NGHTTP2_LIB nghttp2 REQUIRED)
+    find_library(LB9_ZSTD_LIB zstd REQUIRED)
+    get_target_property(_lb9_curl_real CURL::libcurl ALIASED_TARGET)
+    if (NOT _lb9_curl_real)
+        set(_lb9_curl_real CURL::libcurl)
+    endif()
+    set_property(TARGET ${_lb9_curl_real} APPEND PROPERTY
+        INTERFACE_LINK_LIBRARIES "${LB9_NGHTTP2_LIB}" "${LB9_ZSTD_LIB}")
+endif()
 find_package(ICU 78.3 EXACT REQUIRED COMPONENTS data i18n uc)
 find_package(LibXml2 REQUIRED)
 find_package(OpenSSL REQUIRED)
 find_package(SDL3 CONFIG REQUIRED)
+if (PLAN9 AND LB9_SDL3_VJOYSTICK_OBJECT)
+    # PLAN9: virtual-joystick entry points InternalGamepad links, pending
+    # their move into the sysroot sdl3-shim (see plan9-inject).
+    get_target_property(_lb9_sdl3_real SDL3::SDL3 ALIASED_TARGET)
+    if (NOT _lb9_sdl3_real)
+        set(_lb9_sdl3_real SDL3::SDL3)
+    endif()
+    set_property(TARGET ${_lb9_sdl3_real} APPEND PROPERTY
+        INTERFACE_LINK_LIBRARIES "${LB9_SDL3_VJOYSTICK_OBJECT}")
+endif()
 find_package(simdjson CONFIG REQUIRED)
 find_package(SQLite3 REQUIRED)
 find_package(Threads REQUIRED)
@@ -98,6 +142,13 @@ if(unofficial-angle_FOUND)
 else()
     pkg_check_modules(angle REQUIRED IMPORTED_TARGET angle)
     set(ANGLE_TARGETS PkgConfig::angle)
+    if (PLAN9 AND LB9_GL9EGL_OBJECT)
+        # PLAN9: gl9's EGL front end is one cc9 object (gl9egl.app.o, needs
+        # only OSMesa* from libgl9mesa.a), not part of the archive; the
+        # Compositor links its egl* entry points. Path set in plan9-inject.
+        set_property(TARGET PkgConfig::angle APPEND PROPERTY
+            INTERFACE_LINK_LIBRARIES "${LB9_GL9EGL_OBJECT}")
+    endif()
 endif()
 
 if (WIN32)
@@ -141,6 +192,15 @@ else()
 
     pkg_check_modules(skia skia=${SKIA_REQUIRED_VERSION} REQUIRED IMPORTED_TARGET skia)
     set(SKIA_TARGET PkgConfig::skia)
+    if (PLAN9)
+        # PLAN9: static link — skia's FreeType port and Ladybird's harfbuzz
+        # use need the archives on the final link line, and the pathops
+        # module (Op/Simplify) is supplied separately (see plan9-inject).
+        find_library(LB9_FREETYPE_LIB freetype REQUIRED)
+        find_library(LB9_HARFBUZZ_LIB harfbuzz REQUIRED)
+        set_property(TARGET PkgConfig::skia APPEND PROPERTY INTERFACE_LINK_LIBRARIES
+            "${LB9_SKIA_PATHOPS_ARCHIVE}" "${LB9_FREETYPE_LIB}" "${LB9_HARFBUZZ_LIB}")
+    endif()
     set_property(TARGET PkgConfig::skia APPEND PROPERTY INTERFACE_COMPILE_DEFINITIONS "SKCMS_DLL")
 endif()
 add_library(skia ALIAS ${SKIA_TARGET})
diff --git a/Tests/LibIPC/CMakeLists.txt b/Tests/LibIPC/CMakeLists.txt
index 5df47617d6..b791d707af 100644
--- a/Tests/LibIPC/CMakeLists.txt
+++ b/Tests/LibIPC/CMakeLists.txt
@@ -1,4 +1,8 @@
 if (UNIX AND NOT APPLE)
-    ladybird_test("TestTransportSocket.cpp" LibIPC LIBS LibIPC LibSync)
+    if (NOT PLAN9)
+        # Exercises TransportSocket internals (SendQueue, drain windows);
+        # plan9 uses TransportPlan9, covered by the portable TestConnection.
+        ladybird_test("TestTransportSocket.cpp" LibIPC LIBS LibIPC LibSync)
+    endif()
     ladybird_test("TestConnection.cpp" LibIPC LIBS LibIPC)
 endif()
diff --git a/UI/cmake/GUIFramework.cmake b/UI/cmake/GUIFramework.cmake
index 3952280eb5..7817c7512e 100644
--- a/UI/cmake/GUIFramework.cmake
+++ b/UI/cmake/GUIFramework.cmake
@@ -2,6 +2,11 @@
 if (ANDROID OR VCPKG_TARGET_ANDROID)
     set(_possible_guis "Android")
     set(_default_gui "Android")
+elseif (PLAN9)
+    # 9front has no Qt: the chrome is a libdraw presenter driven over a pipe
+    # (UI/Plan9). Headless mode (M4) also lives under this framework.
+    set(_possible_guis "Plan9")
+    set(_default_gui "Plan9")
 elseif (APPLE)
     set(_possible_guis "AppKit" "Qt")
     set(_default_gui "AppKit")
diff --git a/UI/cmake/ResourceFiles.cmake b/UI/cmake/ResourceFiles.cmake
index 99d21fc6ab..48409b8d6b 100644
--- a/UI/cmake/ResourceFiles.cmake
+++ b/UI/cmake/ResourceFiles.cmake
@@ -2,6 +2,16 @@ set(FONTS
     NotoEmoji.ttf
     SerenitySans-Regular.ttf
 )
+if (PLAN9)
+    # ladybird9: Plan 9 has no system fonts, so bundle a normal, broad-coverage
+    # family for the CSS generics instead of falling back to the hand-drawn
+    # SerenitySans (see FontPlugin.cpp PLAN9 arm). DejaVu Sans/Serif/Mono cover
+    # Latin/Cyrillic/Greek + symbols and read normally. Also required: without a
+    # fixed-width family FontPlugin's VERIFY(m_default_fixed_width_font) aborts
+    # WebContent. PLAN9-gated so a same-commit host parity build (which has system
+    # fonts) does not require these ttfs in Base/res/fonts.
+    list(APPEND FONTS DejaVuSans.ttf DejaVuSerif.ttf DejaVuSansMono.ttf)
+endif()
 list(TRANSFORM FONTS PREPEND "${LADYBIRD_SOURCE_DIR}/Base/res/fonts/")
 
 set(16x16_ICONS
diff --git a/Utilities/CMakeLists.txt b/Utilities/CMakeLists.txt
index caa5dadebd..0bc6ca3616 100644
--- a/Utilities/CMakeLists.txt
+++ b/Utilities/CMakeLists.txt
@@ -1,7 +1,7 @@
 ladybird_utility(js SOURCES js.cpp LIBS LibCrypto LibJS LibMain LibTextCodec LibGC)
 ladybird_utility(wasm SOURCES wasm.cpp LIBS LibFileSystem LibWasm LibMain LibJS LibCrypto LibGC)
 
-if (NOT WIN32 AND NOT ANDROID)
+if (NOT WIN32 AND NOT ANDROID AND NOT PLAN9)
     target_link_libraries(js PRIVATE PkgConfig::LIBEDIT)
 endif()
 
