# Build for 9front: custom target + build-std of core/alloc from source (nightly).
[build]
target = "/Users/claw/Projects/agent9/rust9/targets/x86_64-unknown-plan9.json"

[unstable]
build-std = ["core", "alloc"]
