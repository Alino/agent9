--- a/src/util/os_time.c
+++ b/src/util/os_time.c
@@ -48,7 +48,9 @@
 #elif DETECT_OS_WINDOWS
 #  include <windows.h>
 #else
-#  error Unsupported OS
+#  include <unistd.h> /* ported: Plan 9 — usleep + timespec via cc9 libc */
+#  include <time.h>
+#  include <errno.h>
 #endif
 
 
@@ -81,7 +83,7 @@
       Sleep(dwMilliseconds);
    }
 #else
-#  error Unsupported OS
+   usleep(usecs); /* ported: Plan 9 */
 #endif
 }
 
