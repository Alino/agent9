--- a/Include/internal/pycore_initconfig.h
+++ b/Include/internal/pycore_initconfig.h
@@ -21,20 +21,26 @@
 #  define _PyStatus_GET_FUNC() __func__
 #endif
 
+/* plan9: Plan 9's kencc (6c) has no C99 compound literals, so the original
+ * (PyStatus){...} macros don't compile. Build the struct in a static-inline
+ * helper instead; __func__ is captured at the call site by passing it in. */
+static inline PyStatus
+_PyStatus_make(int type, const char *func, const char *err_msg, int exitcode)
+{
+    PyStatus s;
+    s._type = type;
+    s.func = func;
+    s.err_msg = err_msg;
+    s.exitcode = exitcode;
+    return s;
+}
 #define _PyStatus_OK() \
-    (PyStatus){._type = _PyStatus_TYPE_OK,}
-    /* other fields are set to 0 */
+    _PyStatus_make(_PyStatus_TYPE_OK, (const char*)0, (const char*)0, 0)
 #define _PyStatus_ERR(ERR_MSG) \
-    (PyStatus){ \
-        ._type = _PyStatus_TYPE_ERROR, \
-        .func = _PyStatus_GET_FUNC(), \
-        .err_msg = (ERR_MSG)}
-        /* other fields are set to 0 */
+    _PyStatus_make(_PyStatus_TYPE_ERROR, _PyStatus_GET_FUNC(), (ERR_MSG), 0)
 #define _PyStatus_NO_MEMORY() _PyStatus_ERR("memory allocation failed")
 #define _PyStatus_EXIT(EXITCODE) \
-    (PyStatus){ \
-        ._type = _PyStatus_TYPE_EXIT, \
-        .exitcode = (EXITCODE)}
+    _PyStatus_make(_PyStatus_TYPE_EXIT, (const char*)0, (const char*)0, (EXITCODE))
 #define _PyStatus_IS_ERROR(err) \
     (err._type == _PyStatus_TYPE_ERROR)
 #define _PyStatus_IS_EXIT(err) \
@@ -48,7 +54,17 @@
 
 /* --- PyWideStringList ------------------------------------------------ */
 
-#define _PyWideStringList_INIT (PyWideStringList){.length = 0, .items = NULL}
+/* plan9: no compound literals (kencc). All call sites are local-var
+ * initializers, so a static-inline helper is valid. */
+static inline PyWideStringList
+_PyWideStringList_make(void)
+{
+    PyWideStringList l;
+    l.length = 0;
+    l.items = (wchar_t **)0;
+    return l;
+}
+#define _PyWideStringList_INIT _PyWideStringList_make()
 
 #ifndef NDEBUG
 PyAPI_FUNC(int) _PyWideStringList_CheckConsistency(const PyWideStringList *list);
@@ -105,11 +121,19 @@
     int warn_default_encoding;     /* -X warn_default_encoding and PYTHONWARNDEFAULTENCODING */
 } _PyPreCmdline;
 
-#define _PyPreCmdline_INIT \
-    (_PyPreCmdline){ \
-        .use_environment = -1, \
-        .isolated = -1, \
-        .dev_mode = -1}
+/* plan9: no compound literals (kencc). All call sites are local-var
+ * initializers, so a static-inline helper is valid. */
+static inline _PyPreCmdline
+_PyPreCmdline_make(void)
+{
+    _PyPreCmdline c;
+    memset(&c, 0, sizeof(c));
+    c.use_environment = -1;
+    c.isolated = -1;
+    c.dev_mode = -1;
+    return c;
+}
+#define _PyPreCmdline_INIT _PyPreCmdline_make()
 /* Note: _PyPreCmdline_INIT sets other fields to 0/NULL */
 
 extern void _PyPreCmdline_Clear(_PyPreCmdline *cmdline);
--- a/Include/pymacro.h
+++ b/Include/pymacro.h
@@ -147,8 +147,14 @@
 #elif defined(_MSC_VER)
 #  define Py_UNREACHABLE() __assume(0)
 #else
+   /* plan9: kencc has no __builtin_unreachable and (since we neutralize
+    * __attribute__) cannot see Py_FatalError as noreturn, so functions ending
+    * in Py_UNREACHABLE() trip its hard "no return at end of function" error.
+    * kencc *does* treat an infinite loop as non-returning, so add one. */
+#  define _Py_PLAN9_STR(x) #x
+#  define _Py_PLAN9_XSTR(x) _Py_PLAN9_STR(x)
 #  define Py_UNREACHABLE() \
-    Py_FatalError("Unreachable C code path reached")
+    do { Py_FatalError("Unreachable at " __FILE__ ":" _Py_PLAN9_XSTR(__LINE__)); } while (1)
 #endif
 
 // Prevent using an expression as a l-value.
--- a/Include/cpython/compile.h
+++ b/Include/cpython/compile.h
@@ -28,8 +28,16 @@
     int cf_feature_version;  /* minor Python version (PyCF_ONLY_AST) */
 } PyCompilerFlags;
 
-#define _PyCompilerFlags_INIT \
-    (PyCompilerFlags){.cf_flags = 0, .cf_feature_version = PY_MINOR_VERSION}
+/* plan9: no compound literals (kencc). Build via static-inline helper. */
+static inline PyCompilerFlags
+_PyCompilerFlags_make(void)
+{
+    PyCompilerFlags f;
+    f.cf_flags = 0;
+    f.cf_feature_version = PY_MINOR_VERSION;
+    return f;
+}
+#define _PyCompilerFlags_INIT _PyCompilerFlags_make()
 
 /* Future feature support */
 
--- a/Include/object.h
+++ b/Include/object.h
@@ -697,7 +697,8 @@
         default:                                                            \
             Py_UNREACHABLE();                                               \
         }                                                                   \
-    } while (0)
+    } while (1)  /* plan9: while(1) so kencc sees this always-returning macro \
+                  * as non-falling-through (the body never reaches the test) */
 
 
 /*
--- a/Include/internal/pycore_code.h
+++ b/Include/internal/pycore_code.h
@@ -92,6 +92,35 @@
 
 #define INLINE_CACHE_ENTRIES_STORE_SUBSCR CACHE_ENTRIES(_PyStoreSubscrCache)
 
+/* plan9: kencc rounds these all-uint16_t cache structs up to 8-byte multiples,
+ * so CACHE_ENTRIES(sizeof(cache)/2) overcounts (e.g. _PyPrecallCache sizeof is
+ * 8 not 2). The bytecode is laid out by the host with the authoritative counts
+ * from _PyOpcode_Caches, so the runtime cache-skip must match those exactly.
+ * Override with literals (the struct field offsets are unaffected -- kencc only
+ * pads at the end). */
+#undef INLINE_CACHE_ENTRIES_LOAD_GLOBAL
+#define INLINE_CACHE_ENTRIES_LOAD_GLOBAL 5
+#undef INLINE_CACHE_ENTRIES_BINARY_OP
+#define INLINE_CACHE_ENTRIES_BINARY_OP 1
+#undef INLINE_CACHE_ENTRIES_UNPACK_SEQUENCE
+#define INLINE_CACHE_ENTRIES_UNPACK_SEQUENCE 1
+#undef INLINE_CACHE_ENTRIES_COMPARE_OP
+#define INLINE_CACHE_ENTRIES_COMPARE_OP 2
+#undef INLINE_CACHE_ENTRIES_BINARY_SUBSCR
+#define INLINE_CACHE_ENTRIES_BINARY_SUBSCR 4
+#undef INLINE_CACHE_ENTRIES_LOAD_ATTR
+#define INLINE_CACHE_ENTRIES_LOAD_ATTR 4
+#undef INLINE_CACHE_ENTRIES_STORE_ATTR
+#define INLINE_CACHE_ENTRIES_STORE_ATTR 4
+#undef INLINE_CACHE_ENTRIES_LOAD_METHOD
+#define INLINE_CACHE_ENTRIES_LOAD_METHOD 10
+#undef INLINE_CACHE_ENTRIES_CALL
+#define INLINE_CACHE_ENTRIES_CALL 4
+#undef INLINE_CACHE_ENTRIES_PRECALL
+#define INLINE_CACHE_ENTRIES_PRECALL 1
+#undef INLINE_CACHE_ENTRIES_STORE_SUBSCR
+#define INLINE_CACHE_ENTRIES_STORE_SUBSCR 1
+
 #define QUICKENING_WARMUP_DELAY 8
 
 /* We want to compare to zero for efficiency, so we offset values accordingly */
--- a/Parser/pegen.h
+++ b/Parser/pegen.h
@@ -175,14 +175,17 @@
     va_end(va);
     return NULL;
 }
-#define RAISE_SYNTAX_ERROR(msg, ...) _PyPegen_raise_error(p, PyExc_SyntaxError, msg, ##__VA_ARGS__)
-#define RAISE_INDENTATION_ERROR(msg, ...) _PyPegen_raise_error(p, PyExc_IndentationError, msg, ##__VA_ARGS__)
-#define RAISE_SYNTAX_ERROR_KNOWN_RANGE(a, b, msg, ...) \
-    RAISE_ERROR_KNOWN_LOCATION(p, PyExc_SyntaxError, (a)->lineno, (a)->col_offset, (b)->end_lineno, (b)->end_col_offset, msg, ##__VA_ARGS__)
-#define RAISE_SYNTAX_ERROR_KNOWN_LOCATION(a, msg, ...) \
-    RAISE_ERROR_KNOWN_LOCATION(p, PyExc_SyntaxError, (a)->lineno, (a)->col_offset, (a)->end_lineno, (a)->end_col_offset, msg, ##__VA_ARGS__)
-#define RAISE_SYNTAX_ERROR_STARTING_FROM(a, msg, ...) \
-    RAISE_ERROR_KNOWN_LOCATION(p, PyExc_SyntaxError, (a)->lineno, (a)->col_offset, CURRENT_POS, CURRENT_POS, msg, ##__VA_ARGS__)
+/* plan9: kencc's cpp has no GNU ", ## __VA_ARGS__" comma-elision. Fold msg into
+ * __VA_ARGS__ so there is always >=1 variadic arg and no dangling comma -- pure
+ * C99, works in Plan 9 cpp. (msg stays first, semantics unchanged.) */
+#define RAISE_SYNTAX_ERROR(...) _PyPegen_raise_error(p, PyExc_SyntaxError, __VA_ARGS__)
+#define RAISE_INDENTATION_ERROR(...) _PyPegen_raise_error(p, PyExc_IndentationError, __VA_ARGS__)
+#define RAISE_SYNTAX_ERROR_KNOWN_RANGE(a, b, ...) \
+    RAISE_ERROR_KNOWN_LOCATION(p, PyExc_SyntaxError, (a)->lineno, (a)->col_offset, (b)->end_lineno, (b)->end_col_offset, __VA_ARGS__)
+#define RAISE_SYNTAX_ERROR_KNOWN_LOCATION(a, ...) \
+    RAISE_ERROR_KNOWN_LOCATION(p, PyExc_SyntaxError, (a)->lineno, (a)->col_offset, (a)->end_lineno, (a)->end_col_offset, __VA_ARGS__)
+#define RAISE_SYNTAX_ERROR_STARTING_FROM(a, ...) \
+    RAISE_ERROR_KNOWN_LOCATION(p, PyExc_SyntaxError, (a)->lineno, (a)->col_offset, CURRENT_POS, CURRENT_POS, __VA_ARGS__)
 #define RAISE_SYNTAX_ERROR_INVALID_TARGET(type, e) _RAISE_SYNTAX_ERROR_INVALID_TARGET(p, type, e)
 
 Py_LOCAL_INLINE(void *)
--- a/Parser/parser.c
+++ b/Parser/parser.c
@@ -13,63 +13,38 @@
 #  define MAXSTACK 6000
 #endif
 static const int n_keyword_lists = 9;
+/* plan9: kencc has no compound literals; hoist the per-length keyword tables to
+ * named statics instead of (KeywordToken[]){...} inside reserved_keywords[]. */
+static KeywordToken _kw0[] = {{NULL, -1}};
+static KeywordToken _kw1[] = {{NULL, -1}};
+static KeywordToken _kw2[] = {
+    {"if", 639}, {"as", 637}, {"in", 648}, {"or", 574}, {"is", 582},
+    {NULL, -1},
+};
+static KeywordToken _kw3[] = {
+    {"del", 603}, {"def", 649}, {"for", 647}, {"try", 621}, {"and", 575},
+    {"not", 581}, {NULL, -1},
+};
+static KeywordToken _kw4[] = {
+    {"from", 572}, {"pass", 504}, {"with", 612}, {"elif", 641}, {"else", 642},
+    {"None", 601}, {"True", 600}, {NULL, -1},
+};
+static KeywordToken _kw5[] = {
+    {"raise", 522}, {"yield", 573}, {"break", 508}, {"class", 651},
+    {"while", 644}, {"False", 602}, {NULL, -1},
+};
+static KeywordToken _kw6[] = {
+    {"return", 519}, {"import", 531}, {"assert", 526}, {"global", 523},
+    {"except", 634}, {"lambda", 586}, {NULL, -1},
+};
+static KeywordToken _kw7[] = {
+    {"finally", 630}, {NULL, -1},
+};
+static KeywordToken _kw8[] = {
+    {"continue", 509}, {"nonlocal", 524}, {NULL, -1},
+};
 static KeywordToken *reserved_keywords[] = {
-    (KeywordToken[]) {{NULL, -1}},
-    (KeywordToken[]) {{NULL, -1}},
-    (KeywordToken[]) {
-        {"if", 639},
-        {"as", 637},
-        {"in", 648},
-        {"or", 574},
-        {"is", 582},
-        {NULL, -1},
-    },
-    (KeywordToken[]) {
-        {"del", 603},
-        {"def", 649},
-        {"for", 647},
-        {"try", 621},
-        {"and", 575},
-        {"not", 581},
-        {NULL, -1},
-    },
-    (KeywordToken[]) {
-        {"from", 572},
-        {"pass", 504},
-        {"with", 612},
-        {"elif", 641},
-        {"else", 642},
-        {"None", 601},
-        {"True", 600},
-        {NULL, -1},
-    },
-    (KeywordToken[]) {
-        {"raise", 522},
-        {"yield", 573},
-        {"break", 508},
-        {"class", 651},
-        {"while", 644},
-        {"False", 602},
-        {NULL, -1},
-    },
-    (KeywordToken[]) {
-        {"return", 519},
-        {"import", 531},
-        {"assert", 526},
-        {"global", 523},
-        {"except", 634},
-        {"lambda", 586},
-        {NULL, -1},
-    },
-    (KeywordToken[]) {
-        {"finally", 630},
-        {NULL, -1},
-    },
-    (KeywordToken[]) {
-        {"continue", 509},
-        {"nonlocal", 524},
-        {NULL, -1},
-    },
+    _kw0, _kw1, _kw2, _kw3, _kw4, _kw5, _kw6, _kw7, _kw8,
 };
 static char *soft_keywords[] = {
     "_",
--- a/Python/initconfig.c
+++ b/Python/initconfig.c
@@ -277,8 +277,8 @@
 PyStatus PyStatus_Error(const char *err_msg)
 {
     assert(err_msg != NULL);
-    return (PyStatus){._type = _PyStatus_TYPE_ERROR,
-                      .err_msg = err_msg};
+    /* plan9: no compound literals (kencc) -- use the helper. */
+    return _PyStatus_make(_PyStatus_TYPE_ERROR, (const char*)0, err_msg, 0);
 }
 
 PyStatus PyStatus_NoMemory(void)
--- a/Python/pylifecycle.c
+++ b/Python/pylifecycle.c
@@ -99,11 +99,19 @@
 
 #endif
 
+/* plan9: see pystate.c -- undef stdin/stdout/stderr (APE macros) across the
+ * _PyRuntimeState_INIT expansion to dodge a kencc cpp ## bug, then restore. */
+#undef stdin
+#undef stdout
+#undef stderr
 _PyRuntimeState _PyRuntime
 #if defined(__linux__) && (defined(__GNUC__) || defined(__clang__))
 __attribute__ ((section (".PyRuntime")))
 #endif
 = _PyRuntimeState_INIT;
+#define stdin  (&_IO_stream[0])
+#define stdout (&_IO_stream[1])
+#define stderr (&_IO_stream[2])
 _Py_COMP_DIAG_POP
 
 static int runtime_initialized = 0;
--- a/Python/pystate.c
+++ b/Python/pystate.c
@@ -52,7 +52,17 @@
 _Py_COMP_DIAG_IGNORE_DEPR_DECLS
 /* We use "initial" if the runtime gets re-used
    (e.g. Py_Finalize() followed by Py_Initialize(). */
+/* plan9: kencc's cpp wrongly macro-expands stdin/stdout/stderr (APE macros)
+ * when they appear as INIT_ID(...) ## operands deep inside _PyRuntimeState_INIT,
+ * yielding "_ ## (". Undef across the init, then restore APE's definitions so
+ * later fprintf(stderr, ...) still works. */
+#undef stdin
+#undef stdout
+#undef stderr
 static const _PyRuntimeState initial = _PyRuntimeState_INIT;
+#define stdin  (&_IO_stream[0])
+#define stdout (&_IO_stream[1])
+#define stderr (&_IO_stream[2])
 _Py_COMP_DIAG_POP
 
 static int
@@ -1848,7 +1858,8 @@
     PyInterpreterState *interp = tstate->interp;
 
     // Reset data before re-populating.
-    *data = (_PyCrossInterpreterData){0};
+    /* plan9: no compound literals (kencc); zero via memset. */
+    memset(data, 0, sizeof(*data));
     data->free = PyMem_RawFree;  // Set a default that may be overridden.
 
     // Call the "getdata" func for the object.
--- a/Python/pytime.c
+++ b/Python/pytime.c
@@ -44,7 +44,10 @@
 #  error "time_t is not a two's complement integer type"
 #endif
 
-#if _PyTime_MIN + _PyTime_MAX != -1
+/* plan9: kencc's cpp can't evaluate INT64_MIN + INT64_MAX in a #if (the limit
+ * macros use a cast/LL form it rejects), and the syntax error derails the rest
+ * of the file. int64_t is two's complement on amd64, so drop the assertion. */
+#if 0
 #  error "_PyTime_t is not a two's complement integer type"
 #endif
 
--- a/Python/ast_unparse.c
+++ b/Python/ast_unparse.c
@@ -35,7 +35,7 @@
 
 #define APPEND_STR_FINISH(str)  do { \
         return append_charp(writer, (str)); \
-    } while (0)
+    } while (1)  /* plan9: while(1) -- body always returns; satisfies kencc no-return */
 
 #define APPEND_STR(str)  do { \
         if (-1 == append_charp(writer, (str))) { \
--- a/Objects/longobject.c
+++ b/Objects/longobject.c
@@ -345,7 +345,7 @@
             (ival) >>= PyLong_SHIFT; \
         } \
         return (PyObject *)v; \
-    } while(0)
+    } while(1)  /* plan9: while(1) -- body always returns; satisfies kencc no-return */
 
 /* Create a new int object from a C unsigned long int */
 
--- a/Objects/clinic/floatobject.c.h
+++ b/Objects/clinic/floatobject.c.h
@@ -263,28 +263,30 @@
     {"__getformat__", (PyCFunction)float___getformat__, METH_O|METH_CLASS, float___getformat____doc__},
 
 static PyObject *
-float___getformat___impl(PyTypeObject *type, const char *typestr);
+/* plan9: kencc rejects the identifier `typestr` in parameter position; rename
+ * the param (also in the definition in floatobject.c). */
+float___getformat___impl(PyTypeObject *type, const char *typestr_);
 
 static PyObject *
 float___getformat__(PyTypeObject *type, PyObject *arg)
 {
     PyObject *return_value = NULL;
-    const char *typestr;
+    const char *typestr_;  /* plan9: kencc rejects the bare token `typestr` */
 
     if (!PyUnicode_Check(arg)) {
         _PyArg_BadArgument("__getformat__", "argument", "str", arg);
         goto exit;
     }
     Py_ssize_t typestr_length;
-    typestr = PyUnicode_AsUTF8AndSize(arg, &typestr_length);
-    if (typestr == NULL) {
+    typestr_ = PyUnicode_AsUTF8AndSize(arg, &typestr_length);
+    if (typestr_ == NULL) {
         goto exit;
     }
-    if (strlen(typestr) != (size_t)typestr_length) {
+    if (strlen(typestr_) != (size_t)typestr_length) {
         PyErr_SetString(PyExc_ValueError, "embedded null character");
         goto exit;
     }
-    return_value = float___getformat___impl(type, typestr);
+    return_value = float___getformat___impl(type, typestr_);
 
 exit:
     return return_value;
--- a/Objects/dictobject.c
+++ b/Objects/dictobject.c
@@ -453,7 +453,20 @@
  * See https://github.com/python/cpython/pull/127568#discussion_r1868070614
  * for the rationale of using dk_log2_index_bytes=3 instead of 0.
  */
-static PyDictKeysObject empty_keys_struct = {
+/* plan9: kencc/6l cannot statically initialize a flexible array member
+ * (char dk_indices[]). Use a layout-compatible struct with a fixed-size
+ * dk_indices and cast -- the named fields match PyDictKeysObject exactly so the
+ * trailing array sits at the same offset. */
+static struct {
+        Py_ssize_t dk_refcnt;
+        uint8_t dk_log2_size;
+        uint8_t dk_log2_index_bytes;
+        uint8_t dk_kind;
+        uint32_t dk_version;
+        Py_ssize_t dk_usable;
+        Py_ssize_t dk_nentries;
+        char dk_indices[8];
+} empty_keys_struct = {
         1, /* dk_refcnt */
         0, /* dk_log2_size */
         3, /* dk_log2_index_bytes */
@@ -465,7 +478,7 @@
          DKIX_EMPTY, DKIX_EMPTY, DKIX_EMPTY, DKIX_EMPTY}, /* dk_indices */
 };
 
-#define Py_EMPTY_KEYS &empty_keys_struct
+#define Py_EMPTY_KEYS ((PyDictKeysObject *)&empty_keys_struct)
 
 /* Uncomment to check the dict content in _PyDict_CheckConsistency() */
 // #define DEBUG_PYDICT
--- a/Modules/posixmodule.c
+++ b/Modules/posixmodule.c
@@ -14439,7 +14439,9 @@
             PyObject *module = PyType_GetModule(Py_TYPE(iterator));
             entry = DirEntry_from_posix_info(module,
                                              &iterator->path, direntp->d_name,
-                                             name_len, direntp->d_ino
+                                             /* plan9: APE dirent has no d_ino;
+                                              * the inode lives in d_stat. */
+                                             name_len, direntp->d_stat.st_ino
 #ifdef HAVE_DIRENT_D_TYPE
                                              , direntp->d_type
 #endif
--- a/Objects/listobject.c
+++ b/Objects/listobject.c
@@ -1286,13 +1286,18 @@
             *p = *(p-1);
         *l = pivot;
         if (lo.values != NULL) {
-            Py_ssize_t offset = lo.values - lo.keys;
-            p = start + offset;
-            pivot = *p;
-            l += offset;
-            for (p = start + offset; p > l; --p)
-                *p = *(p-1);
-            *l = pivot;
+            /* plan9: keys and values are independent allocations, so the
+               original code's `offset = lo.values - lo.keys` overflows a
+               4-byte Py_ssize_t (Plan 9 amd64: ptr=8, size_t/Py_ssize_t=4).
+               Index both arrays by the same in-array index instead, which is
+               small and always fits. */
+            Py_ssize_t istart = start - lo.keys;
+            Py_ssize_t il = l - lo.keys;
+            Py_ssize_t k2;
+            pivot = lo.values[istart];
+            for (k2 = istart; k2 > il; --k2)
+                lo.values[k2] = lo.values[k2 - 1];
+            lo.values[il] = pivot;
         }
     }
     return 0;
--- a/Modules/expat/expat_config.h
+++ b/Modules/expat/expat_config.h
@@ -6,6 +6,12 @@
 #define EXPAT_CONFIG_H
 
 #include <pyconfig.h>
+/* plan9: our hand-authored pyconfig.h defines PREFIX as the install path
+   ("/sys/lib/python") for getpath.c. Expat uses PREFIX both as a struct
+   typedef and as a function-like namespacing macro, so the object-like
+   macro collides (syntax error in xmlparse.c, redefinition in xmltok.c).
+   Expat does not want CPython's install prefix -- drop it here. */
+#undef PREFIX
 #ifdef WORDS_BIGENDIAN
 #define BYTEORDER 4321
 #else
--- a/Objects/unicodeobject.c
+++ b/Objects/unicodeobject.c
@@ -2901,7 +2901,12 @@
                 len = sprintf(buffer, "%llu", va_arg(*vargs, unsigned long long));
             }
             else if (size_tflag) {
-                len = sprintf(buffer, "%zu", va_arg(*vargs, size_t));
+                /* plan9: APE's printf reads 8 bytes for the `z` length
+                   modifier (native usize), but Py_ssize_t/size_t are 4 bytes
+                   here, so "%zu" prints garbage. size_t == unsigned long == 4
+                   bytes, so format via "%lu" with a matching cast. */
+                len = sprintf(buffer, "%lu",
+                              (unsigned long)va_arg(*vargs, size_t));
             }
             else {
                 len = sprintf(buffer, "%u", va_arg(*vargs, unsigned int));
@@ -2918,7 +2923,10 @@
                 len = sprintf(buffer, "%lli", va_arg(*vargs, long long));
             }
             else if (size_tflag) {
-                len = sprintf(buffer, "%zi", va_arg(*vargs, Py_ssize_t));
+                /* plan9: see the "%zu" note above -- Py_ssize_t == long == 4
+                   bytes, so format via "%li" with a matching cast. */
+                len = sprintf(buffer, "%li",
+                              (long)va_arg(*vargs, Py_ssize_t));
             }
             else {
                 len = sprintf(buffer, "%i", va_arg(*vargs, int));
--- a/Modules/_json.c
+++ b/Modules/_json.c
@@ -1355,14 +1355,17 @@
                     );
             return NULL;
         }
-        if (i > 0) {
+        /* plan9: kencc's float comparison codegen is wrong for the IEEE
+           unordered (NaN) case -- `nan < 0` evaluates true -- so test NaN
+           explicitly first instead of relying on `i < 0` to exclude it. */
+        if (Py_IS_NAN(i)) {
+            return PyUnicode_FromString("NaN");
+        }
+        else if (i > 0) {
             return PyUnicode_FromString("Infinity");
-        }
-        else if (i < 0) {
-            return PyUnicode_FromString("-Infinity");
         }
         else {
-            return PyUnicode_FromString("NaN");
+            return PyUnicode_FromString("-Infinity");
         }
     }
     return PyFloat_Type.tp_repr(obj);
--- a/Modules/_pickle.c
+++ b/Modules/_pickle.c
@@ -1746,8 +1746,11 @@
 
     if (!self->bin) {
         pdata[0] = GET;
+        /* plan9: APE's *printf reads 8 bytes for the `z` length modifier but
+           Py_ssize_t is 4 bytes here, so "%zd" emits a garbage memo index in
+           protocol-0 (text) pickles. Py_ssize_t == long == 4 bytes -> "%ld". */
         PyOS_snprintf(pdata + 1, sizeof(pdata) - 1,
-                      "%zd\n", *value);
+                      "%ld\n", (long)*value);
         len = strlen(pdata);
     }
     else {
@@ -1803,8 +1806,9 @@
     }
     else if (!self->bin) {
         pdata[0] = PUT;
+        /* plan9: see GET note above -- "%zd" is broken under APE printf. */
         PyOS_snprintf(pdata + 1, sizeof(pdata) - 1,
-                      "%zd\n", idx);
+                      "%ld\n", (long)idx);
         len = strlen(pdata);
     }
     else {
--- a/Include/pyhash.h
+++ b/Include/pyhash.h
@@ -21,7 +21,11 @@
    _Py_HashDouble in Python/pyhash.c.  Numeric hashes are based on
    reduction modulo the prime 2**_PyHASH_BITS - 1. */
 
-#if SIZEOF_VOID_P >= 8
+/* plan9: Py_hash_t follows Py_ssize_t (4 bytes here), not void* (8). Selecting
+   the 61-bit modulus by SIZEOF_VOID_P alone overflowed the 32-bit hash, so
+   hash(1.0) != hash(1) -- violating the equal-values-hash-equal invariant and
+   corrupting dict/set with mixed int/float keys. Gate on SIZEOF_SIZE_T too. */
+#if SIZEOF_VOID_P >= 8 && SIZEOF_SIZE_T >= 8
 #  define _PyHASH_BITS 61
 #else
 #  define _PyHASH_BITS 31
--- a/Objects/complexobject.c
+++ b/Objects/complexobject.c
@@ -42,12 +42,24 @@
     return r;
 }
 
+/* plan9: kencc compiles unary `-x` on a double as `0.0 - x`, which loses the
+   sign of zero (-(0.0) == +0.0). Negate via the sign bit so complex negation
+   preserves signed zeros (e.g. -0j, -(0+0j)). See also float_neg. */
+static double
+_plan9_dneg(double x)
+{
+    union { double d; unsigned long long u; } v;
+    v.d = x;
+    v.u ^= 0x8000000000000000ULL;
+    return v.d;
+}
+
 Py_complex
 _Py_c_neg(Py_complex a)
 {
     Py_complex r;
-    r.real = -a.real;
-    r.imag = -a.imag;
+    r.real = _plan9_dneg(a.real);
+    r.imag = _plan9_dneg(a.imag);
     return r;
 }
 
@@ -94,6 +106,15 @@
      const double abs_breal = b.real < 0 ? -b.real : b.real;
      const double abs_bimag = b.imag < 0 ? -b.imag : b.imag;
 
+    /* plan9: kencc commutes `abs_breal >= abs_bimag` to `abs_bimag <= abs_breal`
+       and its `<=` codegen is true for a NaN operand, so a NaN-containing
+       denominator wrongly entered the `abs_breal == 0` -> EDOM branch (raising
+       ZeroDivisionError instead of yielding nan+nanj). Handle NaN up front. */
+    if (Py_IS_NAN(b.real) || Py_IS_NAN(b.imag)) {
+        r.real = r.imag = Py_NAN;
+        return r;
+    }
+
     if (abs_breal >= abs_bimag) {
         /* divide tops and bottom by b.real */
         if (abs_breal == 0.0) {
@@ -364,9 +385,11 @@
     const char *lead = "";
     const char *tail = "";
 
-    if (v->cval.real == 0. && copysign(1.0, v->cval.real)==1.0) {
+    if (v->cval.real == 0. && !Py_IS_NAN(v->cval.real) &&
+            copysign(1.0, v->cval.real)==1.0) {
         /* Real part is +0: just output the imaginary part and do not
-           include parens. */
+           include parens. (plan9: guard NaN -- kencc `nan == 0.` is true,
+           which would drop a NaN real part from the repr.) */
         re = "";
         im = PyOS_double_to_string(v->cval.imag, format_code,
                                    precision, 0, NULL);
@@ -544,8 +567,8 @@
 complex_neg(PyComplexObject *v)
 {
     Py_complex neg;
-    neg.real = -v->cval.real;
-    neg.imag = -v->cval.imag;
+    neg.real = _plan9_dneg(v->cval.real);
+    neg.imag = _plan9_dneg(v->cval.imag);
     return PyComplex_FromCComplex(neg);
 }
 
--- a/Modules/mathmodule.c
+++ b/Modules/mathmodule.c
@@ -651,8 +651,14 @@
             return Py_NAN;
         }
 
-        absx = fabs(x);
-        absy = fabs(y);
+        /* plan9/kencc: fabs(-0.0) does not clear the sign bit, so absx would
+           keep a spurious -0.0 and corrupt the signed-zero result below. Clear
+           the sign bit directly. */
+        {
+            union { double d; unsigned long long u; } ax, ay;
+            ax.d = x; ax.u &= 0x7fffffffffffffffULL; absx = ax.d;
+            ay.d = y; ay.u &= 0x7fffffffffffffffULL; absy = ay.d;
+        }
         m = fmod(absx, absy);
 
         /*
@@ -713,7 +719,17 @@
             assert(m == c);
             r = m - 2.0 * fmod(0.5 * (absx - m), absy);
         }
-        return copysign(1.0, x) * r;
+        /* plan9/kencc: `copysign(1.0, x) * r` computes -1.0 * 0.0 as +0.0,
+           losing the signed zero that remainder(-0.0, y) must return. Apply
+           the sign of x to r by flipping r's sign bit directly. */
+        {
+            union { double d; unsigned long long u; } xv, rv;
+            xv.d = x;
+            rv.d = r;
+            if (xv.u >> 63)
+                rv.u ^= 0x8000000000000000ULL;
+            return rv.d;
+        }
     }
 
     /* Special values. */
@@ -790,6 +806,11 @@
 #else
         double m;
         int e;
+        /* plan9: APE's frexp is wrong for subnormals (negative mantissa), so
+           log2 of a subnormal hit log(<0)=domain error. Scale subnormals up to
+           a normal value first: log2(x) = log2(x*2**54) - 54. */
+        if (x < 2.2250738585072014e-308)
+            return m_log2(x * 18014398509481984.0) - 54.0;
         m = frexp(x, &e);
         /* We want log2(m * 2**e) == log(m) / log(2) + e.  Care is needed when
          * x is just greater than 1.0: in that case e is 1, log(m) is negative,
@@ -1153,6 +1174,24 @@
         return PyFloat_FromDouble(r);
 }
 
+/* plan9: APE's acos sets a spurious EDOM at the +-1 boundary; guard the genuine
+   domain and drop the spurious errno. (exp/cosh/sinh now come from the vendored
+   plan9_libm.c, which handles inf/nan per C99, so they need no wrapper.) */
+static double
+m_acos(double x)
+{
+    double r;
+    if (Py_IS_NAN(x))
+        return x;
+    if (x < -1.0 || x > 1.0) {
+        errno = EDOM;
+        return Py_NAN;                  /* genuine domain error */
+    }
+    r = acos(x);
+    errno = 0;                          /* drop APE's spurious boundary EDOM */
+    return r;
+}
+
 #define FUNC1(funcname, func, can_overflow, docstring)                  \
     static PyObject * math_##funcname(PyObject *self, PyObject *args) { \
         return math_1(args, func, can_overflow);                            \
@@ -1171,7 +1210,7 @@
     }\
     PyDoc_STRVAR(math_##funcname##_doc, docstring);
 
-FUNC1(acos, acos, 0,
+FUNC1(acos, m_acos, 0,
       "acos($module, x, /)\n--\n\n"
       "Return the arc cosine (measured in radians) of x.\n\n"
       "The result is between 0 and pi.")
@@ -1487,7 +1526,9 @@
         }
 
         n = i;                              /* ps[i:] = [x] */
-        if (x != 0.0) {
+        /* plan9: kencc compiles `nan != 0.0` as false, so a NaN x would skip
+           the nonfinite handling and be dropped; admit non-finite x too. */
+        if (x != 0.0 || !Py_IS_FINITE(x)) {
             if (! Py_IS_FINITE(x)) {
                 /* a nonfinite x could arise either as
                    a result of intermediate overflow, or
@@ -1511,7 +1552,9 @@
         }
     }
 
-    if (special_sum != 0.0) {
+    /* plan9: kencc compiles `nan != 0.0` as false, so a NaN special_sum would
+       skip this branch and be dropped; test Py_IS_NAN explicitly. */
+    if (special_sum != 0.0 || Py_IS_NAN(special_sum)) {
         if (Py_IS_NAN(inf_sum))
             PyErr_SetString(PyExc_ValueError,
                             "-inf + inf in fsum");
@@ -2720,7 +2763,10 @@
         x = fabs(px - qx);
         diffs[i] = x;
         found_nan |= Py_IS_NAN(x);
-        if (x > max) {
+        /* plan9: kencc commutes `x > max` to `max < x`, and its `<` codegen
+           returns true for a NaN operand, so a NaN x would wrongly become max
+           (-> nan result for e.g. hypot(inf, nan)). Skip NaN in max-tracking. */
+        if (!Py_IS_NAN(x) && x > max) {
             max = x;
         }
     }
@@ -2773,7 +2819,10 @@
         x = fabs(x);
         coordinates[i] = x;
         found_nan |= Py_IS_NAN(x);
-        if (x > max) {
+        /* plan9: kencc commutes `x > max` to `max < x`, and its `<` codegen
+           returns true for a NaN operand, so a NaN x would wrongly become max
+           (-> nan result for e.g. hypot(inf, nan)). Skip NaN in max-tracking. */
+        if (!Py_IS_NAN(x) && x > max) {
             max = x;
         }
     }
@@ -2861,6 +2910,15 @@
                 r = 0.;
         }
     }
+    else if (x == 0.0 && y < 0.0) {
+        /* plan9: C99 pow(+-0, y<0) is a divide-by-zero -> +-inf, which CPython
+           maps to ValueError. APE's pow instead returns DBL_MAX and sets
+           ERANGE, so the (finite) result slipped past the !isfinite check below
+           and raised OverflowError. Handle it directly. */
+        int odd_y = (fmod(fabs(y), 2.0) == 1.0);
+        errno = EDOM;
+        r = odd_y ? copysign(Py_HUGE_VAL, x) : Py_HUGE_VAL;
+    }
     else {
         /* let libm handle finite**finite */
         errno = 0;
@@ -3019,7 +3077,10 @@
         return -1;
     }
 
-    if ( a == b ) {
+    /* plan9: kencc compiles `a == b` as true for NaN operands, so this
+       short-circuit would wrongly report isclose(nan, nan) as close. Exclude
+       NaN explicitly (NaN is close to nothing, including itself). */
+    if ( a == b && !Py_IS_NAN(a) && !Py_IS_NAN(b) ) {
         /* short circuit exact equality -- needed to catch two infinities of
            the same sign. And perhaps speeds things up a bit sometimes.
         */
--- a/Modules/cmathmodule.c
+++ b/Modules/cmathmodule.c
@@ -139,6 +139,16 @@
 /* forward declarations */
 static Py_complex cmath_asinh_impl(PyObject *, Py_complex);
 static Py_complex cmath_atanh_impl(PyObject *, Py_complex);
+
+/* plan9: sign-bit negation (kencc's unary `-x` is `0.0 - x`, losing -0.0). */
+static double
+_plan9_dneg(double x)
+{
+    union { double d; unsigned long long u; } v;
+    v.d = x;
+    v.u ^= 0x8000000000000000ULL;
+    return v.d;
+}
 static Py_complex cmath_cosh_impl(PyObject *, Py_complex);
 static Py_complex cmath_sinh_impl(PyObject *, Py_complex);
 static Py_complex cmath_sqrt_impl(PyObject *, Py_complex);
@@ -239,11 +249,11 @@
                                M_LN2*2., z.imag);
         } else {
             r.imag = copysign(log(hypot(z.real/2., z.imag/2.)) +
-                              M_LN2*2., -z.imag);
+                              M_LN2*2., _plan9_dneg(z.imag));  /* kencc -0.0 */
         }
     } else {
         s1.real = 1.-z.real;
-        s1.imag = -z.imag;
+        s1.imag = _plan9_dneg(z.imag);  /* plan9: kencc -z.imag loses -0.0 */
         s1 = cmath_sqrt_impl(module, s1);
         s2.real = 1.+z.real;
         s2.imag = z.imag;
@@ -302,11 +312,13 @@
 {
     /* asin(z) = -i asinh(iz) */
     Py_complex s, r;
-    s.real = -z.imag;
+    /* plan9: kencc's unary `-x` loses the sign of zero, corrupting asin's
+       signed-zero branch cuts. Negate via the sign bit. */
+    s.real = _plan9_dneg(z.imag);
     s.imag = z.real;
     s = cmath_asinh_impl(module, s);
     r.real = s.imag;
-    r.imag = -s.real;
+    r.imag = _plan9_dneg(s.real);
     return r;
 }
 
@@ -333,12 +345,12 @@
                               M_LN2*2., z.real);
         } else {
             r.real = -copysign(log(hypot(z.real/2., z.imag/2.)) +
-                               M_LN2*2., -z.real);
+                               M_LN2*2., _plan9_dneg(z.real));  /* kencc -0.0 */
         }
         r.imag = atan2(z.imag, fabs(z.real));
     } else {
         s1.real = 1.+z.imag;
-        s1.imag = -z.real;
+        s1.imag = _plan9_dneg(z.real);  /* plan9: kencc -z.real loses -0.0 */
         s1 = cmath_sqrt_impl(module, s1);
         s2.real = 1.-z.imag;
         s2.imag = z.real;
@@ -363,11 +375,13 @@
 {
     /* atan(z) = -i atanh(iz) */
     Py_complex s, r;
-    s.real = -z.imag;
+    /* plan9: negate via the sign bit -- kencc's unary `-x` is `0.0 - x` and
+       loses the sign of zero, which breaks atan's signed-zero branch cuts. */
+    s.real = _plan9_dneg(z.imag);
     s.imag = z.real;
     s = cmath_atanh_impl(module, s);
     r.real = s.imag;
-    r.imag = -s.real;
+    r.imag = _plan9_dneg(s.real);
     return r;
 }
 
@@ -433,11 +447,11 @@
         */
         h = hypot(z.real/2., z.imag/2.);  /* safe from overflow */
         r.real = z.real/4./h/h;
-        /* the two negations in the next line cancel each other out
-           except when working with unsigned zeros: they're there to
-           ensure that the branch cut has the correct continuity on
-           systems that don't support signed zeros */
-        r.imag = -copysign(Py_MATH_PI/2., -z.imag);
+        /* the two negations cancel except for signed zeros, ensuring correct
+           branch-cut continuity. plan9: -copysign(C, -z.imag) is exactly
+           copysign(C, z.imag), and avoids kencc's unary `-z.imag` which loses
+           the sign of zero (-(+0.0) == +0.0). */
+        r.imag = copysign(Py_MATH_PI/2., z.imag);
         errno = 0;
     } else if (z.real == 1. && ay < CM_SQRT_DBL_MIN) {
         /* C99 standard says:  atanh(1+/-0.) should be inf +/- 0i */
@@ -452,7 +466,11 @@
         }
     } else {
         r.real = m_log1p(4.*z.real/((1-z.real)*(1-z.real) + ay*ay))/4.;
-        r.imag = -atan2(-2.*z.imag, (1-z.real)*(1+z.real) - ay*ay)/2.;
+        /* plan9: the outer unary `-` on atan2's result loses the sign of a
+           zero (kencc compiles -x as 0.0-x), breaking atanh's signed-zero
+           branch-cut continuity (and atan's, which is -i*atanh(iz)). Negate
+           via the sign bit instead. */
+        r.imag = _plan9_dneg(atan2(-2.*z.imag, (1-z.real)*(1+z.real) - ay*ay))/2.;
         errno = 0;
     }
     return r;
@@ -471,7 +489,7 @@
 {
     /* cos(z) = cosh(iz) */
     Py_complex r;
-    r.real = -z.imag;
+    r.real = _plan9_dneg(z.imag);  /* plan9: kencc -z.imag loses -0.0 */
     r.imag = z.real;
     r = cmath_cosh_impl(module, r);
     return r;
@@ -706,11 +724,11 @@
 {
     /* sin(z) = -i sin(iz) */
     Py_complex s, r;
-    s.real = -z.imag;
+    s.real = _plan9_dneg(z.imag);  /* plan9: kencc -x loses -0.0 */
     s.imag = z.real;
     s = cmath_sinh_impl(module, s);
     r.real = s.imag;
-    r.imag = -s.real;
+    r.imag = _plan9_dneg(s.real);
     return r;
 }
 
@@ -864,11 +882,11 @@
 {
     /* tan(z) = -i tanh(iz) */
     Py_complex s, r;
-    s.real = -z.imag;
+    s.real = _plan9_dneg(z.imag);  /* plan9: kencc -x loses -0.0 */
     s.imag = z.real;
     s = cmath_tanh_impl(module, s);
     r.real = s.imag;
-    r.imag = -s.real;
+    r.imag = _plan9_dneg(s.real);
     return r;
 }
 
@@ -1200,7 +1218,11 @@
         return -1;
     }
 
-    if ( (a.real == b.real) && (a.imag == b.imag) ) {
+    /* plan9: kencc compiles `x == y` as true for NaN operands; exclude NaN so
+       isclose(complex(nan), complex(nan)) is correctly not-close. */
+    if ( (a.real == b.real) && (a.imag == b.imag)
+         && !Py_IS_NAN(a.real) && !Py_IS_NAN(a.imag)
+         && !Py_IS_NAN(b.real) && !Py_IS_NAN(b.imag) ) {
         /* short circuit exact equality -- needed to catch two infinities of
            the same sign. And perhaps speeds things up a bit sometimes.
         */
@@ -1294,12 +1316,15 @@
 
 #define INIT_SPECIAL_VALUES(NAME, BODY) { Py_complex* p = (Py_complex*)NAME; BODY }
 #define C(REAL, IMAG) p->real = REAL; p->imag = IMAG; ++p;
+/* plan9: the literal -0. constant-folds to +0.0 under kencc, so every C(NZ,..)
+   special-value table entry stored +0.0. NZ builds a true -0.0 at runtime. */
+#define NZ (_plan9_dneg(0.))
 
     INIT_SPECIAL_VALUES(acos_special_values, {
       C(P34,INF) C(P,INF)  C(P,INF)  C(P,-INF)  C(P,-INF)  C(P34,-INF) C(N,INF)
       C(P12,INF) C(U,U)    C(U,U)    C(U,U)     C(U,U)     C(P12,-INF) C(N,N)
-      C(P12,INF) C(U,U)    C(P12,0.) C(P12,-0.) C(U,U)     C(P12,-INF) C(P12,N)
-      C(P12,INF) C(U,U)    C(P12,0.) C(P12,-0.) C(U,U)     C(P12,-INF) C(P12,N)
+      C(P12,INF) C(U,U)    C(P12,0.) C(P12,NZ) C(U,U)     C(P12,-INF) C(P12,N)
+      C(P12,INF) C(U,U)    C(P12,0.) C(P12,NZ) C(U,U)     C(P12,-INF) C(P12,N)
       C(P12,INF) C(U,U)    C(U,U)    C(U,U)     C(U,U)     C(P12,-INF) C(N,N)
       C(P14,INF) C(0.,INF) C(0.,INF) C(0.,-INF) C(0.,-INF) C(P14,-INF) C(N,INF)
       C(N,INF)   C(N,N)    C(N,N)    C(N,N)     C(N,N)     C(N,-INF)   C(N,N)
@@ -1311,97 +1336,97 @@
       C(INF,-P12) C(U,U)     C(0.,-P12) C(0.,P12) C(U,U)    C(INF,P12) C(N,N)
       C(INF,-P12) C(U,U)     C(0.,-P12) C(0.,P12) C(U,U)    C(INF,P12) C(N,N)
       C(INF,-P12) C(U,U)     C(U,U)     C(U,U)    C(U,U)    C(INF,P12) C(N,N)
-      C(INF,-P14) C(INF,-0.) C(INF,-0.) C(INF,0.) C(INF,0.) C(INF,P14) C(INF,N)
+      C(INF,-P14) C(INF,NZ) C(INF,NZ) C(INF,0.) C(INF,0.) C(INF,P14) C(INF,N)
       C(INF,N)    C(N,N)     C(N,N)     C(N,N)    C(N,N)    C(INF,N)   C(N,N)
     })
 
     INIT_SPECIAL_VALUES(asinh_special_values, {
-      C(-INF,-P14) C(-INF,-0.) C(-INF,-0.) C(-INF,0.) C(-INF,0.) C(-INF,P14) C(-INF,N)
+      C(-INF,-P14) C(-INF,NZ) C(-INF,NZ) C(-INF,0.) C(-INF,0.) C(-INF,P14) C(-INF,N)
       C(-INF,-P12) C(U,U)      C(U,U)      C(U,U)     C(U,U)     C(-INF,P12) C(N,N)
-      C(-INF,-P12) C(U,U)      C(-0.,-0.)  C(-0.,0.)  C(U,U)     C(-INF,P12) C(N,N)
-      C(INF,-P12)  C(U,U)      C(0.,-0.)   C(0.,0.)   C(U,U)     C(INF,P12)  C(N,N)
+      C(-INF,-P12) C(U,U)      C(NZ,NZ)  C(NZ,0.)  C(U,U)     C(-INF,P12) C(N,N)
+      C(INF,-P12)  C(U,U)      C(0.,NZ)   C(0.,0.)   C(U,U)     C(INF,P12)  C(N,N)
       C(INF,-P12)  C(U,U)      C(U,U)      C(U,U)     C(U,U)     C(INF,P12)  C(N,N)
-      C(INF,-P14)  C(INF,-0.)  C(INF,-0.)  C(INF,0.)  C(INF,0.)  C(INF,P14)  C(INF,N)
-      C(INF,N)     C(N,N)      C(N,-0.)    C(N,0.)    C(N,N)     C(INF,N)    C(N,N)
+      C(INF,-P14)  C(INF,NZ)  C(INF,NZ)  C(INF,0.)  C(INF,0.)  C(INF,P14)  C(INF,N)
+      C(INF,N)     C(N,N)      C(N,NZ)    C(N,0.)    C(N,N)     C(INF,N)    C(N,N)
     })
 
     INIT_SPECIAL_VALUES(atanh_special_values, {
-      C(-0.,-P12) C(-0.,-P12) C(-0.,-P12) C(-0.,P12) C(-0.,P12) C(-0.,P12) C(-0.,N)
-      C(-0.,-P12) C(U,U)      C(U,U)      C(U,U)     C(U,U)     C(-0.,P12) C(N,N)
-      C(-0.,-P12) C(U,U)      C(-0.,-0.)  C(-0.,0.)  C(U,U)     C(-0.,P12) C(-0.,N)
-      C(0.,-P12)  C(U,U)      C(0.,-0.)   C(0.,0.)   C(U,U)     C(0.,P12)  C(0.,N)
+      C(NZ,-P12) C(NZ,-P12) C(NZ,-P12) C(NZ,P12) C(NZ,P12) C(NZ,P12) C(NZ,N)
+      C(NZ,-P12) C(U,U)      C(U,U)      C(U,U)     C(U,U)     C(NZ,P12) C(N,N)
+      C(NZ,-P12) C(U,U)      C(NZ,NZ)  C(NZ,0.)  C(U,U)     C(NZ,P12) C(NZ,N)
+      C(0.,-P12)  C(U,U)      C(0.,NZ)   C(0.,0.)   C(U,U)     C(0.,P12)  C(0.,N)
       C(0.,-P12)  C(U,U)      C(U,U)      C(U,U)     C(U,U)     C(0.,P12)  C(N,N)
       C(0.,-P12)  C(0.,-P12)  C(0.,-P12)  C(0.,P12)  C(0.,P12)  C(0.,P12)  C(0.,N)
       C(0.,-P12)  C(N,N)      C(N,N)      C(N,N)     C(N,N)     C(0.,P12)  C(N,N)
     })
 
     INIT_SPECIAL_VALUES(cosh_special_values, {
-      C(INF,N) C(U,U) C(INF,0.)  C(INF,-0.) C(U,U) C(INF,N) C(INF,N)
+      C(INF,N) C(U,U) C(INF,0.)  C(INF,NZ) C(U,U) C(INF,N) C(INF,N)
       C(N,N)   C(U,U) C(U,U)     C(U,U)     C(U,U) C(N,N)   C(N,N)
-      C(N,0.)  C(U,U) C(1.,0.)   C(1.,-0.)  C(U,U) C(N,0.)  C(N,0.)
-      C(N,0.)  C(U,U) C(1.,-0.)  C(1.,0.)   C(U,U) C(N,0.)  C(N,0.)
+      C(N,0.)  C(U,U) C(1.,0.)   C(1.,NZ)  C(U,U) C(N,0.)  C(N,0.)
+      C(N,0.)  C(U,U) C(1.,NZ)  C(1.,0.)   C(U,U) C(N,0.)  C(N,0.)
       C(N,N)   C(U,U) C(U,U)     C(U,U)     C(U,U) C(N,N)   C(N,N)
-      C(INF,N) C(U,U) C(INF,-0.) C(INF,0.)  C(U,U) C(INF,N) C(INF,N)
+      C(INF,N) C(U,U) C(INF,NZ) C(INF,0.)  C(U,U) C(INF,N) C(INF,N)
       C(N,N)   C(N,N) C(N,0.)    C(N,0.)    C(N,N) C(N,N)   C(N,N)
     })
 
     INIT_SPECIAL_VALUES(exp_special_values, {
-      C(0.,0.) C(U,U) C(0.,-0.)  C(0.,0.)  C(U,U) C(0.,0.) C(0.,0.)
+      C(0.,0.) C(U,U) C(0.,NZ)  C(0.,0.)  C(U,U) C(0.,0.) C(0.,0.)
       C(N,N)   C(U,U) C(U,U)     C(U,U)    C(U,U) C(N,N)   C(N,N)
-      C(N,N)   C(U,U) C(1.,-0.)  C(1.,0.)  C(U,U) C(N,N)   C(N,N)
-      C(N,N)   C(U,U) C(1.,-0.)  C(1.,0.)  C(U,U) C(N,N)   C(N,N)
+      C(N,N)   C(U,U) C(1.,NZ)  C(1.,0.)  C(U,U) C(N,N)   C(N,N)
+      C(N,N)   C(U,U) C(1.,NZ)  C(1.,0.)  C(U,U) C(N,N)   C(N,N)
       C(N,N)   C(U,U) C(U,U)     C(U,U)    C(U,U) C(N,N)   C(N,N)
-      C(INF,N) C(U,U) C(INF,-0.) C(INF,0.) C(U,U) C(INF,N) C(INF,N)
-      C(N,N)   C(N,N) C(N,-0.)   C(N,0.)   C(N,N) C(N,N)   C(N,N)
+      C(INF,N) C(U,U) C(INF,NZ) C(INF,0.) C(U,U) C(INF,N) C(INF,N)
+      C(N,N)   C(N,N) C(N,NZ)   C(N,0.)   C(N,N) C(N,N)   C(N,N)
     })
 
     INIT_SPECIAL_VALUES(log_special_values, {
       C(INF,-P34) C(INF,-P)  C(INF,-P)   C(INF,P)   C(INF,P)  C(INF,P34)  C(INF,N)
       C(INF,-P12) C(U,U)     C(U,U)      C(U,U)     C(U,U)    C(INF,P12)  C(N,N)
       C(INF,-P12) C(U,U)     C(-INF,-P)  C(-INF,P)  C(U,U)    C(INF,P12)  C(N,N)
-      C(INF,-P12) C(U,U)     C(-INF,-0.) C(-INF,0.) C(U,U)    C(INF,P12)  C(N,N)
+      C(INF,-P12) C(U,U)     C(-INF,NZ) C(-INF,0.) C(U,U)    C(INF,P12)  C(N,N)
       C(INF,-P12) C(U,U)     C(U,U)      C(U,U)     C(U,U)    C(INF,P12)  C(N,N)
-      C(INF,-P14) C(INF,-0.) C(INF,-0.)  C(INF,0.)  C(INF,0.) C(INF,P14)  C(INF,N)
+      C(INF,-P14) C(INF,NZ) C(INF,NZ)  C(INF,0.)  C(INF,0.) C(INF,P14)  C(INF,N)
       C(INF,N)    C(N,N)     C(N,N)      C(N,N)     C(N,N)    C(INF,N)    C(N,N)
     })
 
     INIT_SPECIAL_VALUES(sinh_special_values, {
-      C(INF,N) C(U,U) C(-INF,-0.) C(-INF,0.) C(U,U) C(INF,N) C(INF,N)
+      C(INF,N) C(U,U) C(-INF,NZ) C(-INF,0.) C(U,U) C(INF,N) C(INF,N)
       C(N,N)   C(U,U) C(U,U)      C(U,U)     C(U,U) C(N,N)   C(N,N)
-      C(0.,N)  C(U,U) C(-0.,-0.)  C(-0.,0.)  C(U,U) C(0.,N)  C(0.,N)
-      C(0.,N)  C(U,U) C(0.,-0.)   C(0.,0.)   C(U,U) C(0.,N)  C(0.,N)
+      C(0.,N)  C(U,U) C(NZ,NZ)  C(NZ,0.)  C(U,U) C(0.,N)  C(0.,N)
+      C(0.,N)  C(U,U) C(0.,NZ)   C(0.,0.)   C(U,U) C(0.,N)  C(0.,N)
       C(N,N)   C(U,U) C(U,U)      C(U,U)     C(U,U) C(N,N)   C(N,N)
-      C(INF,N) C(U,U) C(INF,-0.)  C(INF,0.)  C(U,U) C(INF,N) C(INF,N)
-      C(N,N)   C(N,N) C(N,-0.)    C(N,0.)    C(N,N) C(N,N)   C(N,N)
+      C(INF,N) C(U,U) C(INF,NZ)  C(INF,0.)  C(U,U) C(INF,N) C(INF,N)
+      C(N,N)   C(N,N) C(N,NZ)    C(N,0.)    C(N,N) C(N,N)   C(N,N)
     })
 
     INIT_SPECIAL_VALUES(sqrt_special_values, {
       C(INF,-INF) C(0.,-INF) C(0.,-INF) C(0.,INF) C(0.,INF) C(INF,INF) C(N,INF)
       C(INF,-INF) C(U,U)     C(U,U)     C(U,U)    C(U,U)    C(INF,INF) C(N,N)
-      C(INF,-INF) C(U,U)     C(0.,-0.)  C(0.,0.)  C(U,U)    C(INF,INF) C(N,N)
-      C(INF,-INF) C(U,U)     C(0.,-0.)  C(0.,0.)  C(U,U)    C(INF,INF) C(N,N)
+      C(INF,-INF) C(U,U)     C(0.,NZ)  C(0.,0.)  C(U,U)    C(INF,INF) C(N,N)
+      C(INF,-INF) C(U,U)     C(0.,NZ)  C(0.,0.)  C(U,U)    C(INF,INF) C(N,N)
       C(INF,-INF) C(U,U)     C(U,U)     C(U,U)    C(U,U)    C(INF,INF) C(N,N)
-      C(INF,-INF) C(INF,-0.) C(INF,-0.) C(INF,0.) C(INF,0.) C(INF,INF) C(INF,N)
+      C(INF,-INF) C(INF,NZ) C(INF,NZ) C(INF,0.) C(INF,0.) C(INF,INF) C(INF,N)
       C(INF,-INF) C(N,N)     C(N,N)     C(N,N)    C(N,N)    C(INF,INF) C(N,N)
     })
 
     INIT_SPECIAL_VALUES(tanh_special_values, {
-      C(-1.,0.) C(U,U) C(-1.,-0.) C(-1.,0.) C(U,U) C(-1.,0.) C(-1.,0.)
+      C(-1.,0.) C(U,U) C(-1.,NZ) C(-1.,0.) C(U,U) C(-1.,0.) C(-1.,0.)
       C(N,N)    C(U,U) C(U,U)     C(U,U)    C(U,U) C(N,N)    C(N,N)
-      C(N,N)    C(U,U) C(-0.,-0.) C(-0.,0.) C(U,U) C(N,N)    C(N,N)
-      C(N,N)    C(U,U) C(0.,-0.)  C(0.,0.)  C(U,U) C(N,N)    C(N,N)
+      C(N,N)    C(U,U) C(NZ,NZ) C(NZ,0.) C(U,U) C(N,N)    C(N,N)
+      C(N,N)    C(U,U) C(0.,NZ)  C(0.,0.)  C(U,U) C(N,N)    C(N,N)
       C(N,N)    C(U,U) C(U,U)     C(U,U)    C(U,U) C(N,N)    C(N,N)
-      C(1.,0.)  C(U,U) C(1.,-0.)  C(1.,0.)  C(U,U) C(1.,0.)  C(1.,0.)
-      C(N,N)    C(N,N) C(N,-0.)   C(N,0.)   C(N,N) C(N,N)    C(N,N)
+      C(1.,0.)  C(U,U) C(1.,NZ)  C(1.,0.)  C(U,U) C(1.,0.)  C(1.,0.)
+      C(N,N)    C(N,N) C(N,NZ)   C(N,0.)   C(N,N) C(N,N)    C(N,N)
     })
 
     INIT_SPECIAL_VALUES(rect_special_values, {
-      C(INF,N) C(U,U) C(-INF,0.) C(-INF,-0.) C(U,U) C(INF,N) C(INF,N)
+      C(INF,N) C(U,U) C(-INF,0.) C(-INF,NZ) C(U,U) C(INF,N) C(INF,N)
       C(N,N)   C(U,U) C(U,U)     C(U,U)      C(U,U) C(N,N)   C(N,N)
-      C(0.,0.) C(U,U) C(-0.,0.)  C(-0.,-0.)  C(U,U) C(0.,0.) C(0.,0.)
-      C(0.,0.) C(U,U) C(0.,-0.)  C(0.,0.)    C(U,U) C(0.,0.) C(0.,0.)
+      C(0.,0.) C(U,U) C(NZ,0.)  C(NZ,NZ)  C(U,U) C(0.,0.) C(0.,0.)
+      C(0.,0.) C(U,U) C(0.,NZ)  C(0.,0.)    C(U,U) C(0.,0.) C(0.,0.)
       C(N,N)   C(U,U) C(U,U)     C(U,U)      C(U,U) C(N,N)   C(N,N)
-      C(INF,N) C(U,U) C(INF,-0.) C(INF,0.)   C(U,U) C(INF,N) C(INF,N)
+      C(INF,N) C(U,U) C(INF,NZ) C(INF,0.)   C(U,U) C(INF,N) C(INF,N)
       C(N,N)   C(N,N) C(N,0.)    C(N,0.)     C(N,N) C(N,N)   C(N,N)
     })
     return 0;
--- a/Objects/floatobject.c
+++ b/Objects/floatobject.c
@@ -568,7 +568,15 @@
         goto Unimplemented;
 
  Compare:
-    switch (op) {
+    /* plan9: kencc's float comparison codegen mishandles IEEE unordered (NaN)
+       operands -- `nan == nan` compiles to true, `nan != nan` to false, and
+       `nan < x` to true. Per IEEE-754, every comparison involving a NaN is
+       false except !=. Handle that explicitly so float (and thus dict/set/
+       sort) semantics are correct. */
+    if (Py_IS_NAN(i) || Py_IS_NAN(j)) {
+        r = (op == Py_NE);
+    }
+    else switch (op) {
     case Py_EQ:
         r = i == j;
         break;
@@ -636,7 +644,9 @@
     double a,b;
     CONVERT_TO_DOUBLE(v, a);
     CONVERT_TO_DOUBLE(w, b);
-    if (b == 0.0) {
+    /* plan9: kencc compiles `b == 0.0` as true when b is NaN, which would make
+       x / nan raise ZeroDivisionError instead of returning nan. Exclude NaN. */
+    if (b == 0.0 && !Py_IS_NAN(b)) {
         PyErr_SetString(PyExc_ZeroDivisionError,
                         "float division by zero");
         return NULL;
@@ -652,10 +662,16 @@
     double mod;
     CONVERT_TO_DOUBLE(v, vx);
     CONVERT_TO_DOUBLE(w, wx);
-    if (wx == 0.0) {
+    if (wx == 0.0 && !Py_IS_NAN(wx)) {  /* plan9: kencc nan==0.0 is true */
         PyErr_SetString(PyExc_ZeroDivisionError,
                         "float modulo");
         return NULL;
+    }
+    /* plan9: with a NaN operand the result is NaN, but the `if (mod)` test
+       below is kencc-broken (nan != 0 evaluates false), so it would fall into
+       the signed-zero branch and return 0. Handle NaN up front. */
+    if (Py_IS_NAN(vx) || Py_IS_NAN(wx)) {
+        return PyFloat_FromDouble(Py_NAN);
     }
     mod = fmod(vx, wx);
     if (mod) {
@@ -677,6 +693,13 @@
 _float_div_mod(double vx, double wx, double *floordiv, double *mod)
 {
     double div;
+    /* plan9: NaN operands must yield NaN, but the `if (*mod)`/`if (div)` tests
+       below misfire under kencc (nan != 0 is false), zeroing the result. */
+    if (Py_IS_NAN(vx) || Py_IS_NAN(wx)) {
+        *mod = Py_NAN;
+        *floordiv = Py_NAN;
+        return;
+    }
     *mod = fmod(vx, wx);
     /* fmod is typically exact, so vx-mod is *mathematically* an
        exact multiple of wx.  But this is fp arithmetic, and fp
@@ -718,7 +741,7 @@
     double mod, floordiv;
     CONVERT_TO_DOUBLE(v, vx);
     CONVERT_TO_DOUBLE(w, wx);
-    if (wx == 0.0) {
+    if (wx == 0.0 && !Py_IS_NAN(wx)) {  /* plan9: kencc nan==0.0 is true */
         PyErr_SetString(PyExc_ZeroDivisionError, "float divmod()");
         return NULL;
     }
@@ -733,7 +756,7 @@
     double mod, floordiv;
     CONVERT_TO_DOUBLE(v, vx);
     CONVERT_TO_DOUBLE(w, wx);
-    if (wx == 0.0) {
+    if (wx == 0.0 && !Py_IS_NAN(wx)) {  /* plan9: kencc nan==0.0 is true */
         PyErr_SetString(PyExc_ZeroDivisionError, "float floor division by zero");
         return NULL;
     }
@@ -761,7 +784,10 @@
     CONVERT_TO_DOUBLE(w, iw);
 
     /* Sort out special cases here instead of relying on pow() */
-    if (iw == 0) {              /* v**0 is 1, even 0**0 */
+    /* plan9: kencc compiles `iw == 0` as true when iw is NaN, which would make
+       v**nan return 1 (e.g. (-inf)**nan). Exclude NaN here; the NaN exponent is
+       handled below. */
+    if (iw == 0 && !Py_IS_NAN(iw)) {  /* v**0 is 1, even 0**0 */
         return PyFloat_FromDouble(1.0);
     }
     if (Py_IS_NAN(iv)) {        /* nan**w = nan, unless w == 0 */
@@ -851,8 +877,15 @@
     errno = 0;
     ix = pow(iv, iw);
     _Py_ADJUST_ERANGE1(ix);
-    if (negate_result)
-        ix = -ix;
+    if (negate_result) {
+        /* plan9: kencc compiles unary `-ix` as 0.0-ix, which loses the sign
+           of a zero result (e.g. pow(-2., -2001.) underflows to 0 and must be
+           -0.0). Flip the sign bit directly. */
+        union { double d; unsigned long long u; } nx;
+        nx.d = ix;
+        nx.u ^= 0x8000000000000000ULL;
+        ix = nx.d;
+    }
 
     if (errno != 0) {
         /* We don't expect any errno value other than ERANGE, but
@@ -870,7 +903,14 @@
 static PyObject *
 float_neg(PyFloatObject *v)
 {
-    return PyFloat_FromDouble(-v->ob_fval);
+    /* plan9: kencc compiles unary `-x` on a double as `0.0 - x`, which yields
+       +0.0 for x == 0.0 (IEEE 0-0=+0) -- losing negative zero. Negate by
+       flipping the sign bit so -(0.0) is a true -0.0 (fixes copysign, repr,
+       and other neg-zero semantics). */
+    union { double d; unsigned long long u; } v2;
+    v2.d = v->ob_fval;
+    v2.u ^= 0x8000000000000000ULL;
+    return PyFloat_FromDouble(v2.d);
 }
 
 static PyObject *
@@ -1530,7 +1570,15 @@
         s++;
     if (s != s_end)
         goto parse_error;
-    result = PyFloat_FromDouble(negate ? -x : x);
+    if (negate) {
+        /* plan9: negate via the sign bit -- kencc's `-x` is `0.0 - x`, which
+           yields +0.0 for x == 0.0, so fromhex("-0x0p0") lost the sign. */
+        union { double d; unsigned long long u; } v;
+        v.d = x;
+        v.u ^= 0x8000000000000000ULL;
+        x = v.d;
+    }
+    result = PyFloat_FromDouble(x);
     if (type != &PyFloat_Type && result != NULL) {
         Py_SETREF(result, PyObject_CallOneArg((PyObject *)type, result));
     }
@@ -1754,15 +1802,17 @@
 [clinic start generated code]*/
 
 static PyObject *
-float___getformat___impl(PyTypeObject *type, const char *typestr)
+float___getformat___impl(PyTypeObject *type, const char *typestr_)
 /*[clinic end generated code: output=2bfb987228cc9628 input=d5a52600f835ad67]*/
 {
     float_format_type r;
+    /* plan9: kencc rejects the bare token `typestr` in a declarator, so the
+     * param is typestr_ and we use it directly (no `typestr` alias). */
 
-    if (strcmp(typestr, "double") == 0) {
+    if (strcmp(typestr_, "double") == 0) {
         r = double_format;
     }
-    else if (strcmp(typestr, "float") == 0) {
+    else if (strcmp(typestr_, "float") == 0) {
         r = float_format;
     }
     else {
@@ -2080,7 +2130,10 @@
     unsigned short bits;
     int incr = 1;
 
-    if (x == 0.0) {
+    /* plan9: kencc compiles `x == 0.0` as true when x is NaN (IEEE-unordered
+       compare bug), which would pack a NaN as +0.0. Exclude NaN here so it
+       reaches the Py_IS_NAN branch below. */
+    if (x == 0.0 && !Py_IS_NAN(x)) {
         sign = (copysign(1.0, x) == -1.0);
         e = 0;
         bits = 0;
--- a/Python/dtoa.c
+++ b/Python/dtoa.c
@@ -2126,7 +2126,16 @@
     }
 
   ret:
-    result = sign ? -dval(&rv) : dval(&rv);
+    /* plan9: rv holds a non-negative magnitude; apply the sign via the sign
+       bit, not unary `-` (kencc's `-x` is `0.0 - x`, losing -0.0, so strtod of
+       "-0.0" returned +0.0). */
+    result = dval(&rv);
+    if (sign) {
+        union { double d; unsigned long long u; } _sv;
+        _sv.d = result;
+        _sv.u |= 0x8000000000000000ULL;
+        result = _sv.d;
+    }
     goto done;
 
   parse_error:
@@ -2139,7 +2148,13 @@
     goto done;
 
   undfl:
-    result = sign ? -0.0 : 0.0;
+    if (sign) {
+        union { double d; unsigned long long u; } _sv;
+        _sv.u = 0x8000000000000000ULL;   /* -0.0 */
+        result = _sv.d;
+    } else {
+        result = 0.0;
+    }
     goto done;
 
   ovfl:
