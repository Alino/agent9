host/__pycache__/

# regenerable C++23 module BMIs (host/build-modules.sh)
lib/modules/

# regenerable build archives (build-runtime.sh / build-libm.sh) — too big for git
lib/libcc9cxx.a
lib/libcc9m.a

# archive backups (ar-patch snapshots)
lib/libcc9cxx.a.*

# release artifacts: the tarball is a GitHub release asset, and .cache holds the
# natively built on-box tools carried over from the previous release
release/*.tar.gz
release/.cache/
