/* memory.h — the SVID/legacy alias for <string.h>. Old C/C++ (DOSBox, MAME
 * cores) includes it unguarded; every declaration it ever had lives in
 * string.h. */
#ifndef _MEMORY_H
#define _MEMORY_H
#include <string.h>
#endif
